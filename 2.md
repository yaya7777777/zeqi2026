# 二、网络和 HTTP

---

## 1. 简答题

---

### 1.1 HTTP 和 HTTPS 是什么？HTTP 请求和响应分别由哪些部分组成？

---

#### **一、HTTP 是什么？**

> **全名拆开记**
> HTTP = **H**yper**T**ext **T**ransfer **P**rotocol
> = 超文本传输协议
>
> - "超文本"：不只是纯文本，还包含图片、音频、视频、超链接等形式
> - "传输协议"：浏览器和服务器之间说话的"约定语言"
> - 工作在 **应用层**（OSI 第 7 层），底层基于 TCP 协议（三次握手）

**定义**：HTTP 是互联网上应用最广泛的一种**应用层协议**，用于在 Web 浏览器（客户端）和 Web 服务器之间传输超文本数据（HTML、图片、JSON 等）

**特点**：
1. **无状态**：服务器不会记住上一次请求是谁发的。每个请求都是独立的，所以才需要 Cookie/Session/Token 的帮助来"记住用户"
2. **明文传输**：数据在网络中裸奔，中间节点可以随意截获并阅读（所以非常不安全，诞生了 HTTPS）
3. **基于请求-响应模型**：客户端先发请求 → 服务器处理 → 响应，一次请求对应一次响应

---

#### **二、HTTPS 是什么？**

> **对比HTTP记忆**
> HTTPS = HTTP + **S**ecure
> = 安全的 HTTP
>
> "S" 是通过 **TLS/SSL 协议** 实现的加密层
> 可以理解为：HTTP 是明文传输，谁都能看，不安全；HTTPS 是加密传输，只有接收方能看到，安全

**定义**：HTTPS 是在 HTTP 的基础上，增加了 **TLS（或旧版 SSL）加密层** 的安全版 HTTP 协议（TLS介于应用层和传输层之间）：

**HTTPS做了3件事**：

| 功能 | 说明 | 解决的问题 |
|---|---|---|
| **加密（Confidentiality）** | 数据传输前用对称密钥加密，即使被截获也看不懂 | 防止被窃听（如网吧/公共WiFi被看个人密码） |
| **完整性（Integrity）** | 用哈希算法给数据做“指纹”（一串独一无二的字符串，如 SHA-256），接收方校验指纹是否一致 | 防止被篡改（如运营商在网页里插广告） |
| **身份认证（Authentication）** | 服务器提供数字证书（CA颁发），证明"你访问的确实是 taobao.com，不是钓鱼网站" | 防止被伪装（如 DNS劫持到假网站） |

**HTTP vs HTTPS 对比速记表**：

| 对比项 | HTTP | HTTPS |
|---|---|---|
| 默认端口 | 80 | 443 |
| 传输方式 | 明文 | TLS 加密 |
| URL 前缀 | `http://` | `https://` |
| 安全性 | 不安全 | 安全 |
| 性能开销 | 略快（无握手） | 稍慢（多了 TLS 握手 2~3 RTT） |
| 证书 | 不需要 | 需要 CA 颁发的数字证书（要钱，但也有免费的） |

> **TLS 握手过程：**
> 1. 客户端发 Hello（支持加密套件列表、随机数1）
> 2. 服务器回 Hello（选择加密套件、随机数2、下发数字证书）
> 3. 客户端验证证书，生成"预主密钥"，用服务器公钥加密后发送
> 4. 服务器解密"预主密钥"，用服务器私钥解密后，得到"预主密钥"
> 5. 双方用 随机数1+随机数2+预主密钥 计算出**对称会话密钥**
> 6. 双方用对称会话密钥，发送**Finished**给对方，证明"我已收到你的数据"

---

#### **三、HTTP 请求由哪些部分组成？**

> **请求有3段=请求行+请求头+请求体**
> 头和体之间用一个**空行**隔开

```http
POST /login HTTP/1.1              ←── ① 请求行（3个部分：请求方法 + URL + 版本）
Host: api.example.com             ←──┐
Content-Type: application/json    ←──┤ ② 请求头（多行 Key: Value）
Content-Length: 57                ←──┤
User-Agent: Chrome/120...         ←──┤
Authorization: Bearer xxx...      ←──┘
                                  ←── 空行（分隔请求头和请求体，HTTP 协议规定必须有！）
{                                 ←──┐
  "username": "alice",            ←──┤ ③ 请求体（Body）：携带的业务数据
  "password": "123456"            ←──┤   but GET 请求通常没有 Body
}                                 ←──┘
```

**① 请求行（Request Line）—— 3 个部分空格分隔**：
```
请求方法   请求URL              HTTP版本
POST      /login               HTTP/1.1
```
- **请求方法**：GET / POST / PUT / DELETE / PATCH 等（RESTful 会用到）
- **请求 URL**：资源路径，可以带查询参数（如 `/users?page=1&size=10`）
- **HTTP 版本**：现在基本是 HTTP/1.1，HTTP/2 和 HTTP/3 正在普及

**② 请求头（Headers）—— 每行一个Key: Value**：

| 常见请求头 | 作用（举例） |
|---|---|
| `Host` | 服务器域名（一台服务器可能托管多个网站，必须传） |
| `Content-Type` | 请求体格式：`application/json` / `application/x-www-form-urlencoded` / `multipart/form-data`（文件上传） |
| `Content-Length` | 请求体字节长度 |
| `Authorization` | 身份凭证：`Bearer <token>` 或 `Basic base64(user:pass)` |
| `Cookie` | 浏览器自动携带的Cookie字符串 |
| `User-Agent` | 浏览器/客户端标识（Chrome、Postman、curl 等） |
| `Accept` | 客户端能接收的响应类型：`application/json` / `text/html` 等 |
| `Referer` | 从哪个页面跳转过来的（防盗链会用到） |

**③ 请求体（Body）**：
- GET/DELETE 方法**通常没有 Body**（但协议没有真的禁止，只是约定俗成）
- POST/PUT/PATCH 方法常用，里面装业务数据（JSON格式最常见）

---

#### **四、HTTP 响应由哪些部分组成？**

> **响应也是3段=状态行+响应头+响应体**
> 和请求几乎一样，唯一区别是**第一行**：请求是"请求方法+URL+版本"，响应是"版本+状态码+原因短语"

```http
HTTP/1.1 200 OK                    ←── ① 状态行（版本 + 状态码 + 原因短语）
Content-Type: application/json     ←──┐
Content-Length: 86                 ←──┤ ② 响应头（多行 Key: Value）
Set-Cookie: session=abc123...      ←──┤
Cache-Control: max-age=3600        ←──┘
                                   ←── 空行
{                                 ←──┐
  "code": 0,                       ←──┤ ③ 响应体（Body）：携带的业务数据
  "message": "success",            ←──┤
  "data": { "id": 1, "name":"A" }  ←──┤
}                                  ←──┘
```

**① 状态行（Status Line）**：
```
HTTP版本   状态码   原因短语
HTTP/1.1   200     OK
```

> **常见状态码**：
> | 类别 | 代表 | 含义 |
> |---|---|---|
> | **2xx 成功** | 200 OK | 请求成功（最常见） |
> | | 201 Created | 资源创建成功（POST 注册/新建时用，RESTful规范） |
> | | 204 No Content | 成功但没有响应体（DELETE成功返回） |
> | **3xx 重定向** | 301 Moved Permanently | 永久重定向（域名迁移、SEO） |
> | | 302 Found | 临时重定向 |
> | | 304 Not Modified | 资源未修改，走浏览器缓存（节省带宽） |
> | **4xx 客户端错** | 400 Bad Request | 请求参数/格式错误（通用客户端错） |
> | | 401 Unauthorized | 未登录 / Token 无效 / 没有身份凭证 |
> | | 403 Forbidden | 已登录，但**没有权限**访问（和 401 区分！） |
> | | 404 Not Found | URL 不存在 / 资源找不到 |
> | | 405 Method Not Allowed | 请求方法不对（该 URL 只支持 GET，你发了 POST） |
> | | 409 Conflict | 资源冲突（如注册时用户名已存在） |
> | **5xx 服务器错** | 500 Internal Server Error | 服务器代码崩了（空指针、数据库挂了） |
> | | 502 Bad Gateway | 网关错误（Nginx 转发不到后端，后端没启动） |
> | | 503 Service Unavailable | 服务不可用（服务器过载/维护中） |
> | | 504 Gateway Timeout | 网关超时（后端处理太慢，超过Nginx等待时间） |

**② 响应头**：

| 常见响应头 | 作用 |
|---|---|
| `Content-Type` | 响应体格式：`application/json`、`text/html`、`image/png` 等 |
| `Set-Cookie` | 服务器让浏览器写入 Cookie（下次请求自动带上） |
| `Cache-Control` | 缓存策略：`max-age=3600` 表示浏览器缓存1小时 |
| `Location` | 重定向目标 URL（配合 301/302 使用） |
| `Access-Control-Allow-Origin` | CORS 跨域头：允许哪些域名跨域访问 |

**③ 响应体**：实际业务数据（JSON / HTML / 图片二进制等）

---

### 1.2 GET 与 POST 有什么区别？分别在什么场景下使用？


> **GET 查，POST 增（改）**

---

#### **一、核心区别对比表**

| 对比维度 | GET | POST |
|---|---|---|
| **1. 语义（最本质区别）** | 用于**获取/查询**资源，不应该修改服务器数据 | 用于**提交/创建/修改**数据，会改变服务器状态 |
| **2. 参数位置** | 参数拼接在 **URL 后面**（Query String）<br>例：`/search?q=手机&page=1` | 参数放在 **请求体 Body** 里（JSON/表单） |
| **3. 长度限制** | 浏览器对 URL 有长度限制，传不了大参数 | **理论上无限制**（实际上服务器会配置，如Nginx默认1MB，可改） |
| **4. 安全性** | 参数暴露在 URL 上，**非常不安全** 浏览器历史记录、服务器日志、Referer 都会泄露 | 参数在Body里，相对隐蔽（注意：都不是加密，HTTPS才是真的比较安全！） |
| **5. 可缓存性** | 响应可以被浏览器缓存（相同URL下次直接读本地缓存，更快进行请求） | 默认不缓存（除非手动加 `Cache-Control` 头） |
| **6. 可收藏为书签** | 可以（URL 里带完整参数）比如可以收藏一个搜索结果页 | 不行（Body 参数不在 URL 里） |
| **7. 浏览器刷新/回退** | 无害（只是再查一次） | **会重复提交**！（所以电商支付页要做防重复提交的操作——token或重定向） |
| **8. 编码方式** | 只能 URL 编码（`application/x-www-form-urlencoded`） | 支持多种编码：JSON、表单、文件上传 `multipart/form-data` 等 |
| **9. 幂等性** | 幂等（请求1次和100次结果一样） | 非幂等（提交10次注册=创建10个账号） |

> **幂等性是什么？**
> 一个操作执行一次和执行 N 次，得到的结果完全相同，就叫"幂等"。
> - GET /user/1：查10次还是同一个用户 → 幂等
> - POST /register：提交10次注册10个用户 → 不幂等
> - PUT /user/1：把 name 改成 Bob，改10次还是 Bob → 幂等（PUT本身就是幂等的）
> - DELETE /user/1：删10次，第一次成功后续会出现404，但最终状态还是"已删除" → 幂等


---

#### **二、使用场景**

**用 GET 的场景（查）**：
- 搜索、筛选、分页列表（如商品列表、文章列表）
- 详情页（查看某商品、某用户）
- 任何**只读**、不修改数据库的操作
- 需要收藏、分享链接的场景

**用 POST 的场景（增/改/敏感数据）**：
- 用户注册、登录（密码不能暴露在 URL！）
- 提交表单（创建订单、发布文章）
- 上传文件（图片、文档）
- 提交的数据比较长（比如一段长文本文章）
- 任何**会修改服务器数据**的操作


> "GET 和 POST 最本质的区别是**语义**：GET 用于读取资源，是幂等的；POST 用于创建/提交数据，不幂等。
> 在此基础上衍生出其他区别：参数位置上，GET 拼在URL后面，POST放Body里；因GET 有长度限制、不安全、可缓存可收藏，POST则相反。
> 实际使用中，查询操作用GET，提交/修改/敏感数据统一用POST

---

### 1.3 为什么 API 返回数据普遍使用 JSON，而不是 XML？

> **简单就是美**
> 早期 Web 服务主流是 XML（SOAP 协议时代），2010 年前后JSON崛起并全面胜出。
> 只要对比一下两边写同样数据的代码，就会发现json真的要简单特别多

---

#### **一、对比 JSON 和 XML 的数据表示**

**JSON 版本**（短小精悍）：
```json
{
  "id": 101,
  "username": "zhangsan",
  "age": 22,
  "hobbies": ["coding", "reading", "gaming"],
  "isVip": true
}
```

**XML 版本**（又长又啰嗦）：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<user>
  <id>101</id>
  <username>zhangsan</username>
  <age>22</age>
  <hobbies>
    <hobby>coding</hobby>
    <hobby>reading</hobby>
    <hobby>gaming</hobby>
  </hobbies>
  <isVip>true</isVip>
</user>
```

>可见：XML标签有大量冗余字符，使得同样内容 **XML 体积比 JSON 大 30%~50%**，网络传输更慢、更耗流量

---

#### **二、核心原因**

| # | 原因 | 详细解释 |
|---|---|---|
| **1** | **更简洁，数据量小** | XML有开闭标签 `<name></name>` 完全多余；JSON键值对写法紧凑。省带宽 = 加载快 = 用户体验好 + 服务器省流量费 |
| **2** | **和 JavaScript 天生兼容** | JSON = **J**ava**S**cript **O**bject **N**otation |
| | | 前端JS解析JSON只要一行：`const obj = JSON.parse(str)`<br>解析 XML 要写一堆 `getElementsByTagName()`、`getAttribute()`，非常麻烦<br>这是前端全面支持 JSON 的**最根本原因** |
| **3** | **数据类型支持更直观** | JSON 原生区分 `string` / `number` / `boolean` / `null` / `array` / `object`<br>XML 里**所有内容都是字符串**！`<age>22</age>` 和 `<name>22</name>` 看起来一样，程序还要自己转类型，容易出Bug |
| **4** | **解析性能更高** | JSON 语法简单，解析器写起来简单，速度更快<br>XML 语法复杂，有命名空间、Schema、DTD 等一大堆概念，解析慢且耗内存 |
| **5** | **可读性更好** | JSON 的缩进格式和 Python/JS 的对象字面量几乎一样，看着亲切熟悉<br>XML 标签嵌套过多时人眼很难快速找到数据（如果看到10层嵌套的XML，想死的心都有了） |
| **6** | **RESTful API 潮流推动** | 2007年左右REST风格兴起，主打"简单、轻量、基于 HTTP"<br>JSON 的轻量特性和REST完美契合；而 XML 经常和复杂笨重的SOAP/WSDL绑定在一起，被一并淘汰了

---

#### **三、那 XML 还有哪些地方在用？/全说 JSON 好会不会太绝对？**

XML 虽然 API 接口不用了，但在这些领域仍然是主流：

- **配置文件**：Maven 的 `pom.xml`、Android 的 `AndroidManifest.xml`、Spring 的早期配置文件
  - 原因：XML 支持**注释** `<!-- -->`（JSON 标准不支持注释，这是 JSON 最大的痛点！），而且支持属性和层级更灵活
- **复杂文档标记**：Word (.docx)、Excel (.xlsx) 本质是 XML 压缩包
- **SVG 矢量图**：一种 XML 格式的图片
- **企业级遗留系统**：银行、电信等老旧系统的 SOAP WebService 还在用 XML（历史遗留）


---

### 1.4 简述 Cookie、Session、Token 的使用场景。

> 这三个都是为了解决同一个问题——**HTTP 是无状态的**
> 什么叫无状态？服务器面对发来的10000个请求，不知道哪些是"同一个用户"发来的。
> Cookie/Session/Token 就是三种"确定用户身份"的方案，出现顺序是：
> **Cookie（浏览器存） → Session（服务器存） → JWT Token（双方都不存，签名验证）**

---

#### **一、Cookie —— 浏览器存储**

| 项目 | 说明 |
|---|---|
| **怎么用？存哪里？** | 服务器通过 `Set-Cookie` 响应头发给浏览器，浏览器存在本地（硬盘/内存） |
| **大小限制** | 单条Cookie约 **4KB**，一个域名下约20~50 条 |
| **自动携带吗？** | 浏览器发请求时会自动把该域名的Cookie放在 `Cookie` 请求头上，不用手动加 |
| **安全属性** | 看下面三条 |

**Cookie 的三个安全属性**：
-  `HttpOnly`：禁止JS通过 `document.cookie` 读取（防止偷Cookie）
-  `Secure`：只在HTTPS连接下才发送（防 HTTP明文被截获）
-  `SameSite`：`Strict`/`Lax`/`None` 限制跨站请求携带Cookie（防CSRF（跨站请求伪造）攻击）

**使用场景**：
1. **不敏感的个性化设置**：网站主题色、语言偏好、最近浏览记录
2. **配合 Session**：Cookie里存一个 `sessionId`，真正的数据在服务器（见下文 Session）
3. **广告追踪**：第三方Cookie记录你浏览过什么商品，投放对应广告（现在被浏览器逐渐禁用了）

**不适合存什么**：
- 用户密码、余额等敏感信息（Cookie存在本地，能被用户看到/篡改，很危险）

---

#### **二、Session —— 服务器存储**

| 项目 | 说明 |
|---|---|
| **核心思想** | 敏感数据**不发给浏览器**，存在服务器，只给浏览器一个"快递取件码"sessionId |
| **sessionId存哪里？** | 一般存在Cookie里（叫 `JSESSIONID` 或 `PHPSESSID`）<br>移动端没有Cookie也可以放 URL 参数里（不推荐，容易泄露） |
| **真实数据存哪里？** | 单机部署 → 服务器内存<br>集群部署 → Redis（多台服务器共享同一个Session存储） |

**使用场景**：
1. **Web网站登录**：传统电商、论坛、管理后台（浏览器环境下最经典的方案，省心）
2. **需要服务端主动踢人下线**：直接删Redis 里的sessionId对应用户数据即可生效（这是 Session相比JWT（第三种）的最大优势）

**缺点**：
- 分布式集群需要**共享Session存储**（Redis），否则用户在 A 机登录，下一跳负载均衡到 B 机就不认识了
- 占用服务器内存 / Redis 资源（用户量大了 Redis 压力大）

> **Cookie + Session配合流程：**
> ```
> ① 用户 POST /login {用户名+密码} → 服务器
> ② 服务器验证通过 → 在Redis存 {"sessionId_abc123": {userId: 101, username: "张三"}}
> ③ 服务器响应头加上 → Set-Cookie: SESSIONID=abc123; HttpOnly; Secure
> ④ 浏览器保存 SESSIONID=abc123
> ⑤ 用户访问 GET /user/profile → 浏览器自动带上Cookie: SESSIONID=abc123
> ⑥ 服务器查Redis → 找到 {userId:101...} → 知道是张三，返回他的资料
> ```

---

#### **三、Token（主流是JWT）—— 双方都不存，纯靠签名**

> **什么是JWT？**
> JWT = **J**SON **W**eb **T**oken
> 本质是**三段 Base64 编码 + 签名**的字符串，用 `.` 分隔：
> `头部.载荷.签名` → `eyJhbGciOiJIUzI1NiJ9.eyJ1c2VySWQiOjEwMX0.xxxxxx`
>
> 不同点：**服务器不用存**，服务器拿到Token自己验签，验签通过就相信里面的内容

| 项目 | 说明 |
|---|---|
| **数据存在哪？** | **存在客户端**（LocalStorage / SessionStorage / Cookie / App 本地数据库） |
| **谁来验证？** | 服务器用自己保存的**密钥**验证签名（HMAC-SHA256 或 RSA）<br>签名对得上 → 内容没被篡改，可信 |
| **怎么传递的？** | 一般放在请求头：`Authorization: Bearer <token>`里<br>（这样就没有CSRF风险，因为不会像Cookie一样被浏览器自动携带） |

**使用场景**：
1. **跨域 / 多端登录**：前后端分离项目、App（iOS/Android）、小程序
   - 跨域名时，Cookie很麻烦（需要单独配置），Token放Header里就完全没这种问题
2. **SSO 单点登录**：一个账号登录多个系统（如阿里系：淘宝登录了天猫自动登）
3. **微服务之间的调用鉴权**：服务A调用服务 B，用Token证明身份
4. **第三方授权**：OAuth2（"用微信登录本站"那个跳转流程，本质就是拿Token）

**缺点**：
1. **无法主动失效**：Token 签发后默认有效期内一直有效，用户改密码/被踢下线，旧Token还能用（除非加黑名单，那本质上又回到Session方案了）
2. **载荷不能放敏感信息**：Base64不是加密，任何人拿到Token都能解码看到payload（密码、身份证号等敏感信息绝对不能塞进去）
3. **Token 体积比 sessionId 大**：每次请求都带上，多了点带宽开销（但是也不是很大）

> **JWT 验签原理（非对称加密）：**
> ```
> 签发时：base64(头) + "." + base64(载荷) → 用**私钥**签名 → 得到JWT字符串
> 验证时：收到JWT → 拆分三段 → 用**公钥**验证签名是否合法 → 合法则相信payload内容
> ```

---

#### **四、三者对比总结**

| 对比项 | Cookie | Session | JWT Token |
|---|---|---|---|
| 存储位置 | 浏览器 | 服务器（内存/Redis） | 客户端（LocalStorage等） |
| 服务端存储？ | 不存（纯浏览器存储） | 必须存 | 不存（纯验签） |
| 安全性 | 有 HttpOnly/SameSite 保护 | 安全（数据不外露） | 不怕 CSRF；注意payload别放敏感数据 |
| 跨域支持 | 麻烦（需配置 SameSite=None 等） | 更麻烦（需要共享存储） | 天生支持（放 Header 里） |
| 主动踢下线 | 浏览器清 Cookie 即可 | 直接删服务端 session | 麻烦（需加黑名单/短过期时间+刷新Token） |
| 适用场景 | 小网站、个性化设置 | 传统 Web、需要强控制的系统 | 前后端分离、App、小程序、微服务、SSO |


---

### 1.5 什么是 RESTful API？

> REST 是一种设计风格，而不是强制规范：
> **资源（Resource） + 动词（HTTP Method） + 状态码（HTTP Status）**
> 核心思想：**用HTTP协议本身的语义来表达业务含义，而不是URL随意乱写并将POST用于一切操作**

---

#### **一、非REST风格的反例**

**传统 RPC 风格接口（每个操作一个 URL，POST 万能）**：
```
POST  /getUserById?id=1          # 查，GET 语义却用 POST
POST  /createUser                # 新增
POST  /updateUser                # 修改
POST  /deleteUser                # 删除
POST  /listUsers                 # 查列表
POST  /loginUser                 # 登录
```

问题：
1. URL 里充满了动词（get/create/update/delete），命名混乱，每个人写法都不一样
2. 所有操作都用POST，HTTP的方法形同虚设
3. 错误全返回 200 OK + `{"code":500,"msg":"xxx"}`，状态码失去意义

---

**RESTful 风格**（URL全是名词，用HTTP的方法区分动作）：
```
GET     /users              # 查询用户列表（GET = 查）
GET     /users/1            # 查询 id=1 的用户详情（GET = 查）
POST    /users              # 创建新用户（POST = 增）
PUT     /users/1            # 全量修改 id=1 的用户（PUT = 改）
PATCH   /users/1            # 部分修改 id=1 的用户（PATCH = 局部改）
DELETE  /users/1            # 删除 id=1 的用户（DELETE = 删）
POST    /auth/login         # 登录（不是 CRUD，用动作型 URL 没问题）
```

---

#### **二、REST的设计原则**

> REST = **Re**presentational **S**tate **T**ransfer
> 翻译为：**表现层状态转移**（依旧看不懂）

| # | 原则 | 通俗解释 | 怎么做 |
|---|---|---|---|
| **1** | **资源为中心**<br>Resource-Oriented | URL 只放**名词**（资源名），**不要放动词** | `/users` `/users/1`**YES**<br>`/getUser` `/deleteUser/1`**NO** |
| **2** | **用HTTP方法表示动作**<br>Uniform Interface | CRUD对应到 HTTP方法上，语义一致 | GET=查，POST=增，PUT=全改，PATCH=部分改，DELETE=删 |
| **3** | **用HTTP状态码表示结果**<br>Stateless（不对，状态码是原则2的一部分） | 成功用 2xx，客户端错 4xx，服务器错 5xx<br>不要所有情况都返回 200 | 查询成功→200，创建成功→201，删除成功→204<br>未登录→401，没权限→403，找不到→404，冲突→409 |
| **4** | **无状态**<br>Stateless | 服务器**不保存客户端的上下文状态**<br>每个请求必须自带所有身份信息（Token/SessionId） | 不能依赖服务器的session，每个请求自己带身份凭证<br>好处：水平扩展非常easy，任何一台服务器都能处理任何请求 |
| **5** | **可缓存**<br>Cacheable | 响应会显式标出能不能缓存、缓存多久 | GET接口加 `Cache-Control: max-age=3600`<br>重复查询省带宽、省服务器 |
| **6** | **分层系统**<br>Layered System | 客户端不知道自己连的是哪一层 | 请求路径：Client → Nginx（负载均衡）→ 网关 → 业务服务 → DB<br>每一层只知道下一层，解耦 |

---

#### **三、HTTP 方法和 CRUD 的对应关系**

| HTTP 方法 | 对应数据库 CRUD | 是否幂等 | 典型 URL | 说明 |
|---|---|---|---|---|
| **GET** | Read（查） | 幂等 | `GET /users` `GET /users/1` | 查列表、查单条，请求体通常为空 |
| **POST** | Create（增） | 不幂等 | `POST /users` | 新增一条资源，返回 201 Created + Location 头（可选） |
| **PUT** | Update（全量改） | 幂等 | `PUT /users/1` | 用请求体的**完整数据**覆盖整个资源（即使只改了一个字段，也要把所有字段都传） |
| **PATCH** | Update（部分改） | 不幂等（严格说） | `PATCH /users/1` | 只传要修改的字段（如只改用户名就只传 username）<br>**实际项目里 PATCH 用得少，基本都用 POST 代替** |
| **DELETE** | Delete（删） | 幂等 | `DELETE /users/1` | 删除指定资源，成功返回 204 No Content（无响应体） |

> **PUT vs PATCH 的区别**：
> 用户 `{id:1, name:"张三", age:20}` 现在要把年龄改成 21：
> - **PUT**：请求体必须是完整的 `{id:1, name:"张三", age:21}`（缺字段可能会被置空）
> - **PATCH**：请求体可以只传修改的部分 `{age:21}`
> 实际开发中，很多人为了简单，全用POST或全用PUT做修改，PATCH很鸡肋

---

#### **四、RESTful的实践（设计一个 checklist）**

**URL 设计**：
- 用**复数名词**：`/users` 而不是 `/user`
- 层级关系用斜杠：`GET /users/1/orders` —— 查 id=1 用户的所有订单
- 查询参数放 URL 后面：`GET /users?page=1&size=10&role=admin`
- 不用下划线 `_`，用连字符 `-` 分词：`/user-profiles`（不是 `user_profiles`）
- 全部**小写**（URL大小写敏感，避免歧义）

 **请求体 / 响应体**：
- 统一用 JSON格式（`Content-Type: application/json`）
- 时间用 ISO8601 字符串：`"createdAt": "2024-03-15T14:30:00Z"`，不要用时间戳或自定义格式
- 统一响应结构（可选）：
  ```json
  {
    "code": 0,          // 业务码，0=成功，非0=失败
    "message": "ok",    // 人类可读信息
    "data": { ... }     // 实际业务数据（列表就放数组）
  }
  ```

**版本管理（有三种，任选其一）**：
| 方式 | 示例 | 优缺点 |
|---|---|---|
| URL 路径（最常用） | `GET /api/v1/users` | 简单直观，推荐|
| 请求头 | `Accept: application/vnd.company.v1+json` | URL很干净，但调试麻烦 |
| 查询参数 | `GET /users?version=1` | 最简单，但不太"REST" |

---

#### **五、RESTful的常见不完美场景**

实际项目不会 100% 严格遵守 REST，遇到以下情况可以灵活处理：**一切为了简单**

| 场景 | 纯 REST 方案 | 现实中常用方案 |
|---|---|---|
| 用户登录 | 没有"登录资源"，纯 REST 不好建模 | 直接 `POST /auth/login` 没问题，动作型 URL 接受 |
| 批量删除（删多个 ID） | `DELETE /users/1,2,3`（不优雅） | `POST /users/batch-delete` + body 带 ids 数组 |
| 复杂搜索（多条件、时间范围） | `GET /users?k1=v1&k2=v2&k3=v3...`（URL 会超长） | `POST /users/search` + body 里传复杂 JSON 查询条件 |


## 2. API设计：用户管理系统

---

### 题目背景

> 你正在开发一个"用户管理系统"，能够完成基本的 CRUD 操作。请先完成它的 RESTful API 设计。
>
> - **核心实体**：用户（唯一用户名，含密码）
> - **核心功能**：7 个功能
>   1. 用户注册
>   2. 用户登录
>   3. 查看当前登录用户的信息
>   4. 查看用户列表
>   5. 修改用户名
>   6. 修改密码
>   7. 注销账号

---

### 设计说明

在看具体 API 之前，先理解几个设计决策：

**1. Base URL 版本管理**：统一以 `/api/v1` 开头，方便未来升级到v2时不影响老用户

**2. URL 分层**：
- `/api/v1/auth/*`：认证相关（注册、登录）—— 这些接口**不需要登录**
- `/api/v1/users/*`：用户资源相关（查、改、删）—— 这些接口**需要登录**（`Authorization: Bearer <token>`）

**3. 统一响应体结构**：
```json
{
  "code": 0,          // 业务码：0=成功，其他=失败（和HTTP状态码互补，方便前端统一处理）
  "message": "ok",    // 用户可读的提示
  "data": { ... }     // 实际业务数据，失败时可为 null
}
```
> 虽然纯REST不喜欢在body里再放一层code，但实际上90%项目都这么做，更实用

**4. 需要登录的接口都要求**：
- 请求头：`Authorization: Bearer <JWT Token>`
- Token 无效/过期 → 返回 `401 Unauthorized`

---

### 1. 用户注册

- **请求方法**：`POST`
- **URL**：`/api/v1/auth/register`

> "创建用户"用POST
> 放在 /auth/下 注册和登录一样是"认证流程的入口"，不需要先登录

#### 请求体

```json
{
  "username": "zhangsan",
  "password": "StrongPass123!"
}
```


#### 状态码和响应体

- **`201 Created`**：注册成功（创建成功用201 ）
```json
{
  "code": 0,
  "message": "注册成功",
  "data": {
    "id": 1001,
    "username": "zhangsan",
    "createdAt": "2026-09-03T10:00:00Z"
  }
}
```

- **`400 Bad Request`**：参数校验失败（用户名长度不够、密码太短等）
```json
{
  "code": 40001,
  "message": "密码长度不能少于8位",
  "data": null
}
```

- **`409 Conflict`**：用户名已存在（用409 表示资源冲突）
```json
{
  "code": 40901,
  "message": "用户名已存在",
  "data": null
}
```

---

### 2. 用户登录

- **请求方法**：`POST`
- **URL**：`/api/v1/auth/login`

> "登录"用POST
> 放在 /auth/下 登录和注册一样是"认证流程的入口"，不需要先登录

#### 请求体

```json
{
  "username": "zhangsan",
  "password": "StrongPass123!"
}
```

#### 状态码和响应体

- **`200 OK`**：登录成功（用200 表示成功，返回一个 JWT Token 给前端保存）
```json
{
  "code": 0,
  "message": "登录成功",
  "data": {
    "accessToken": // JWT Token 字符串，前端存 localStorage，后续请求放 `Authorization` 头
    "tokenType": "Bearer",// 固定 "Bearer"，告诉前端怎么拼 Header
    "expiresIn": 7200,// 有效期（秒），7200秒 = 2小时，到期需要重新登录或刷新
    "user": {
      "id": 1001,
      "username": "zhangsan"
    }
  }
}
```

- **`401 Unauthorized`**：用户名或密码错误（用401 表示认证失败）
```json
{
  "code": 40101,
  "message": "用户名或密码错误",
  "data": null
}
```

- **`423 Locked`**：用423 表示账号被锁定/封禁）
```json
{
  "code": 42301,
  "message": "账号已被封禁，请联系管理员",
  "data": null
}
```

---

### 3. 查看当前登录用户的信息

- **请求方法**：`GET`
- **URL**：`/api/v1/users/me`

> `/me` 是一个REST约定俗成的特殊写法，表示"当前认证的用户"
> 比让前端传 `/users/{自己的id}` 更好，避免伪造别人的id正好弄对了

- **需要登录**
- **请求头**：`Authorization: Bearer <token>`

#### 请求体

无（GET 方法没有请求体）

#### 状态码和响应体

- **`200 OK`**：成功（**绝不能返回 password 字段**）
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1001,
    "username": "zhangsan",
    "createdAt": "2026-09-03T10:00:00Z",
    "updatedAt": "2026-09-03T10:00:00Z"
  }
}
```

- **`401 Unauthorized`**：未登录 / Token 过期（用401 表示认证失败）
```json
{
  "code": 40100,
  "message": "请先登录",
  "data": null
}
```

---

### 4. 查看用户列表

- **请求方法**：`GET`
- **URL**：`/api/v1/users`

> 这是管理员接口（需要 admin权限），普通用户调用应该返回 403
> 分页参数放在 Query String（URL 后面），因为 GET 不用 Body

- **需要登录**：（且需要 admin 权限）
- **请求头**：`Authorization: Bearer <admin token>`

#### 请求参数（Query Parameters）

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| page | int | **NO** | 1 | 页码，从 1 开始 |
| size | int | **NO** | 10 | 每页条数，最大 100（以防一次拉太多错过了） |
| username | string | **NO** | 无 | 用户名模糊搜索（困难） |

URL 示例：`GET /api/v1/users?page=1&size=10&username=zhang`

#### 请求体

无（GET 方法没有请求体）

#### 状态码和响应体

- **`200 OK`**：成功
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "username": "zhangsan",
        "createdAt": "2026-09-03T10:00:00Z"
      },
      {
        "id": 1002,
        "username": "lisi",
        "createdAt": "2026-09-02T15:00:00Z"
      }
    ],
    "total": 156,        // 总记录数
    "page": 1,           // 当前页
    "size": 10,          // 每页大小
    "totalPages": 16     // 总页数
  }
}
```

- **`401 Unauthorized`**：未登录
```json
{
  "code": 40100,
  "message": "请先登录",
  "data": null
}
```

- **`403 Forbidden`**：已登录但不是管理员（用403 表示权限不足）
```json
{
  "code": 40300,
  "message": "无权限访问，仅管理员可查看用户列表",
  "data": null
}
```

---

### 5. 修改用户名

- **请求方法**：`PUT`（或 `PATCH`，一般用PUT）
- **URL**：`/api/v1/users/me/username`


> 因为只改"用户名"这一个属性，所以URL要定位到me下面的username
> 当然也可以用 `PUT /users/me` 传完整用户对象，但没必要

- **需要登录**
- **请求头**：`Authorization: Bearer <token>`

#### 请求体

```json
{
  "username": "zhangsan_new",
  "password": "StrongPass123!"
}
```
>**密码要二次检验！！**
>**用户名是唯一的**
#### 状态码和响应体

- **`200 OK`**：修改成功
```json
{
  "code": 0,
  "message": "用户名修改成功",
  "data": {
    "id": 1001,
    "username": "zhangsan_new",
    "updatedAt": "2026-09-03T12:00:00Z"
  }
}
```

- **`400 Bad Request`**：参数校验失败（新用户名格式不对）
```json
{
  "code": 40002,
  "message": "新用户名格式不合法",
  "data": null
}
```

- **`401 Unauthorized`**：密码二次校验错误
```json
{
  "code": 40102,
  "message": "原密码错误",
  "data": null
}
```

- **`409 Conflict`**：新用户名已被别人占用
```json
{
  "code": 40901,
  "message": "用户名已存在",
  "data": null
}
```

---

### 6. 修改密码

- **请求方法**：`PUT`
- **URL**：`/api/v1/users/me/password`
一

- **需要登录**
- **请求头**：`Authorization: Bearer <token>`

#### 请求体

```json
{
  "oldPassword": "StrongPass123!",
  "newPassword": "NewStrongPass456!",
  "confirmPassword": "NewStrongPass456!"
}
```
>**密码要两次输入！**（防止用户不小心输入错了）
>**新密码不能和原密码相同**

#### 状态码和响应体

- **`200 OK`**：修改成功（修改密码后应该**让旧 Token 立即失效**，强制重新登录）
```json
{
  "code": 0,
  "message": "密码修改成功，请重新登录",
  "data": null
}
```

- **`400 Bad Request`**：
  - 两次输入的新密码不一致
  - 新密码格式不合法
  - 新密码和原密码相同
```json
{
  "code": 40003,
  "message": "两次输入的新密码不一致",
  "data": null
}
```

- **`401 Unauthorized`**：原密码错误
```json
{
  "code": 40103,
  "message": "原密码错误",
  "data": null
}
```

---

### 7. 注销账号

- **请求方法**：`DELETE`
- **URL**：`/api/v1/users/me`

> DELETE用来删除（注销）当前用户
> 注销 = 删除用户资源，是永久操作（和退出登录不同）

- **需要登录**
- **请求头**：`Authorization: Bearer <token>`

#### 请求体

```json
{
  "password": "StrongPass123!"
}
```
>**注销的时候密码要二次检验**
>**注销账号后，用户无法再登录**

#### 状态码和响应体

- **`204 No Content`**：注销成功
```
无响应体（204 的语义就是"没有Body"）
```


- **`401 Unauthorized`**：密码二次校验失败
```json
{
  "code": 40104,
  "message": "密码错误，账号注销失败",
  "data": null
}
```

---



### 设计总结

1. **严格遵守 REST 语义**：GET 查、POST 增、PUT 改、DELETE 删，URL 全是名词
2. **版本管理**：`/api/v1` 前缀，方便后续升级
3. **URL分层结构**：`/auth/*` 不需要登录，`/users/*` 需要登录，职责清晰
4. **完全利用状态码**：201 创建成功、204 删除成功、401 未登录、403 没权限、409 冲突，语义精准
5. **安全防护**：
   - 响应体永远不返回 password
   - 改用户名/改密码/注销账号 三大敏感操作，都**二次校验原密码**
   - 改密码后提示重新登录（旧 Token 失效）
   - 登录失败不说是用户名错还是密码错（防枚举）

