# 三、关系型数据库与 SQL

---

## 1. 简答题

---

### 1.1 主键、外键是什么？它们的作用和区别？

- 为什么叫关系型数据库？其实就是靠外键把表和表关联起来
- 主键是自己的身份证号，用来唯一标识每一行记录
- 外键是别人的身份证号，用来建立两张表之间的关联关系

---

#### 一、主键（Primary Key，简称 PK）

**定义**：主键是表中**用来唯一标识每一行记录**的一个字段（或多个字段组合）

> e.g：人有身份证号，每个学生有学号，每门课程有课程号，这些都是"主键"

**主键的硬性要求**：

| 要求 | 说明 | 违反会怎样？ |
|---|---|---|
| ① **唯一性（Unique）** | 同一个表中，任意两行的主键值不能相同 | 插入重复主键 → `1062 Duplicate entry` 报错 |
| ② **非空（Not Null）** | 主键值不允许为 NULL | 插入/更新主键为 NULL → 报错 |
| ③ **不可变（默认约定）** | 某行的主键值一旦确定，尽量不要修改 | 改主键会导致所有关联它的外键失效，维护起来很噩梦了 |
| ④ **一张表只能有一个主键** | 但一个主键可以由多个字段组成（叫联合主键/复合主键） | 定义多个PRIMARY KEY → 语法报错 |

**主键的常见形式**：

| 类型 | 例子 | 适用场景 |
|---|---|---|
| **业务主键**（天然存在） | 学生表的学号 `sno`、身份证号 `id_card` | 业务上本身就稳定存在且唯一 |
| **代理主键**（人造的） | 自增INT主键 `id`、UUID、雪花算法ID | 表没有合适的天然唯一字段时使用（**实际项目用这种**：更简单） |

- 为什么几乎都用自增 ID 作为主键，而不是业务主键（如学号）？
- 答：学号看似唯一，但将来可能遇到"学生转校复用学号"、"管理系统合并导致学号冲突"等业务变化
   代理主键（自增 ID）和业务完全无关，不随业务变化而变化，更稳定且intchar 索引性能更高

---

#### 二、外键（Foreign Key，简称 FK）

**定义**：外键是A表中的一个字段，它**引用了 B 表的主键**，用来建立两张表之间的"关联关系"

>e.g：在大学的学生证上有一个学院编号字段，这个学院编号就是学院表的主键，所以，这个字段就是外键
> 它的作用就是拿着这个编号去学院表查，就能知道属于哪个学院

**外键的作用**：

| 作用 | 通俗说明 | 具体效果 |
|---|---|---|
| ① **约束数据一致性**（"不能乱引用不存在的东西"） | 你不能在选课表里插入一条"学号=S9999"的成绩，因为 S9999 根本没注册过，不存在这个学生 | 插入/更新外键值 → 如果被引用表没有对应主键 → 报错拒绝<br>删除被引用表的主键行 → 如果还有其他表引用它 → 报错拒绝 |
| ② **建立表之间的关联，方便 JOIN 查询** | 通过学生表的 `dept_id`（外键）关联学院表的 `id`（主键），就能JOIN查询"张三所在学院的院长是谁" | SQL 里写 `FROM student s JOIN dept d ON s.dept_id = d.id` |

**外键的动作行为**：

| 配置 | 含义（以"学生表→学院表"外键为例） |
|---|---|
| `RESTRICT`（默认） | 如果学生表里还有人属于这个学院，就**禁止删除**这个学院 |
| `CASCADE`（级联） | 删除学院 → **自动把该学院下所有学生一起删了**（but一般业务不希望级联删主数据） |
| `SET NULL`（置空） | 删除学院 → 学生的 `dept_id` 自动变成 NULL |
| `NO ACTION` | 和 RESTRICT 基本一样|

- 但是**互联网大厂几乎都"逻辑外键"，而不用"物理外键"**
 就是说数据库层面 `FOREIGN KEY` 都**不写**外键语法，但逻辑上要知道 `student.dept_id` 关联 `dept.id`，关联一致性是由**业务代码**控制的
- 为什么这样呢？
 1. 高并发场景下，外键检查会影响性能（每次 INSERT 都要查被引用表）
 2. 分库分表后，跨库的物理外键根本用不了
 3. 批量删改数据时，外键约束可能导致意外锁表、死锁等问题


---

#### 三、主键 vs 外键区别对比表

| 对比维度 | 主键 Primary Key | 外键 Foreign Key |
|---|---|---|
| **作用** | 唯一标识本表中的每一行（自己的身份证） | 关联其他表的主键，建立表间关系（别人的身份证号） |
| **数量** | 一张表**最多 1 个** | 一张表可以有**多个**（比如选课表同时有sno外键和cno外键） |
| **唯一性** | 必须唯一 | 可以重复（同一个学生可以选多门课 →sno重复出现） |
| **是否允许 NULL** | 绝对不允许 | 可以为 NULL（比如某些学生暂时还没分配学院，dept_id 可以为 NULL） |
| **能引用别人吗？** | 不引用其他表 | 必须引用另一个表的主键（或唯一键） |
| **被别人引用吗？** | 经常被其他表的外键引用 | 一般不被引用（除非更复杂的三层关系了） |

---

### 1.2 什么是范式？1NF、2NF、3NF 分别要求什么？

> 范式（Normal Form，简称 NF）是衡量表结构设计得好不好的一套标准。
> 等级越高（1NF < 2NF < 3NF < BCNF < ...），就代表了数据冗余越少、设计越规范

---

#### 一、什么是范式？

**定义**：范式是关系型数据库中，为了**消除数据冗余（重复存储）、消除修改/插入/删除异常**，而定义的一系列表结构设计规则

> 什么是"数据冗余"和"异常"？
> 反例：设计一张巨无霸表存学生选课（显然这个表很不舒服，因为有重复人，重复课，重复成绩）
> | sno学号 | sname姓名 | dept学院 | cno课号 | cname课程名 | grade成绩 |
> |---|---|---|---|---|---|
> | S101 | 张三 | 计算机学院 | C001 | 数据库系统 | 85 |
> | S101 | 张三 | 计算机学院 | C002 | 数据结构 | 90 |
> | S101 | 张三 | 计算机学院 | C003 | 操作系统 | 88 |
> | S102 | 李四 | 软件学院 | C001 | 数据库系统 | 70 |

**三大异常**：
1. **插入异常**：新开设了一门课"编译原理"，但还没有任何学生选过 → 这门课的 cname根本存不进去（因为sno是主键的一部分，不能NULL）
2. **删除异常**：S102退了C001 → 你把这行删掉 → "数据库系统"这门课的名字也被一起删掉了（不应该这样做）
3. **更新异常**："数据库系统"改名叫"数据库原理" → 所有选了这门课的行都要改 cname，漏改一行就数据不一致

范式就是解决这些问题的：把一张大表拆成学生表、课程表、选课表三张，就什么问题都没了

**三大范式**：
- **1NF**：属性不可再分（原子性）
- **2NF**：消除部分依赖（非主属性完全依赖主键）
- **3NF**：消除传递依赖（非主属性不依赖其他非主属性）

- 在讲 2NF 和 3NF 之前，先补一个前置概念：
  **函数依赖**：A 字段能唯一确定 B 字段，就说 B 函数依赖于 A，记作 A → B。
  例：sno → sname，sno → dept（知道学号就能查出唯一的姓名和学院）
  **完全依赖 vs 部分依赖**：
  - 如果主键是 (sno, cno) 联合主键：
   - grade → 必须 sno+cno 一起才能确定（缺一个都不行）= **完全依赖**
   - sname → 只要 sno 就能确定，不需要 cno = **部分依赖**（这就是2NF要消灭的）

---

#### 二、1NF（第一范式）—— 属性的原子性

**要求**：表中每一列（字段）的值都必须是**不可再分的最小单元**，不能是列表、集合、JSON 对象等"拆得开"的东西

| 不满足 1NF（一列存了多个值，可再分） | 满足 1NF（拆成n列，每列都是原子） |
|---|---|
| **"地址"列**：浙江省杭州市余杭区xxx路 | 省：浙江省 / 市：杭州市 / 区：余杭区 / 详细地址：xxx路 |
| **"联系方式"列**：13800138000, 0571-88888888 | 手机号：13800138000 / 固定电话：0571-88888888 |
| **"爱好"列**：篮球, 阅读, 编程 | 爱好是多值，应该拆成单独的"学生爱好表"（一对多关系） |


---

#### 三、2NF（第二范式）—— 消除部分函数依赖

**前置**：首先要已经满足 1NF。
**要求**：表中的所有"非主属性"都**完全依赖于整张主键**，不能只依赖主键的一部分。

> 什么时候会出现部分依赖？
> 只有当表的主键是"**联合主键（多个字段拼起来）**"的时候，才可能出现部分依赖
> 如果整张表的主键就是单个自增id，那这张表自动满足 2NF，都不用再检查

**反例**（就是上面那张选课巨无霸表，主键 = (sno, cno)）：

| 联合主键的字段 | 非主属性 | 是否完全依赖主键？ |
|---|---|---|
| (sno, cno) | grade成绩 | 是！缺任何一个都查不出成绩（完全依赖） |
| (sno, cno) | sname姓名 | 否！只需要 sno 就知道姓名（部分依赖 sno，和 cno 没关系） |
| (sno, cno) | dept学院 | 否！只需要 sno 就知道学院（部分依赖 sno） |
| (sno, cno) | cname课程名 | 否！只需要 cno 就知道课程名（部分依赖 cno） |

**怎么改（怎么消除部分依赖）**？
**拆表！！**
- 学生表（sno 主键 → 存 sname, dept 等"学生的属性"）
- 课程表（cno 主键 → 存 cname 等"课程的属性"）
- 选课表（sno + cno 联合主键 → 只存 grade，因为 grade 是学生选这门课的结果，完全依赖联合主键）
这样拆完之后，三张表都满足 2NF 了

---

#### 四、3NF（第三范式）—— 消除传递函数依赖

**前置**：首先要已经满足 2NF。
**要求**：表中的所有"非主属性"之间不能有函数依赖关系
意思是**非主属性只能依赖主键，不能依赖另一个非主属性**

> 什么是"传递依赖"？
> A → B，B → C，所以 A → C。这是**传递**过去的依赖，C 间接依赖 A
> 3NF 不允许这种间接，要求所有非主属性都**直接依赖主键**

**反例**：满足2NF但不满足3NF的学生表（主键 = sno）：

| sno（主键） | sname | dept学院名 | dept_address学院地址 |
|---|---|---|---|
| S101 | 张三 | 计算机学院 | 清水河校区主楼 |
| S102 | 李四 | 计算机学院 | 清水河校区主楼 |
| S103 | 王五 | 软件学院 | 沙河校区软件楼 |
| S104 | 赵六 | 软件学院 | 沙河校区软件楼 |

这里的函数依赖关系：
- sno → dept学院名 （直接依赖主键，没问题）
- dept学院名 → dept_address学院地址（非主属性依赖另一个非主属性）
- sno → dept学院名 → dept_address （传递依赖）

**问题**：同一个学院有 500 个学生，学院地址就重复存了 500 遍，非常冗余
如果学院搬迁改地址，要改 500 行（更新异常）

**怎么改**？
**拆表！！**
- 学生表（sno PK，sname，**dept_id FK**）← 把学院信息移出去，只留一个外键
- 学院表（dept_id PK，dept_name，dept_address）← 学院信息只存一份

拆完就满足 3NF 了

**所以解决以上问题的方法，都是拆表，尽量让一个表中满足一对多关系**

---

#### 五、范式实践中的一些问题


理论上要求越范式越好
但实际项目中通常**只到 3NF 就停了**（再往上 BCNF/4NF 过于严格反而影响查询性能，因为JOIN 太多）
甚至有时为了性能会**故意违反 3NF 加冗余字段**，这叫"反范式设计"（比如订单表冗余存一份用户昵称，免得每次查订单都JOIN用户表）
原则是**先范式消除异常，性能扛不住时再适度反范式冗余**

---

### 1.3 什么是事务？ACID 分别是什么含义？

> 事务是"要么全做，要么全不做"的一群 SQL 打包执行的工作单元
> 最经典例子就是银行转账：A 扣 100 + B 加 100，这两条 SQL 必须都成功，不能一个成功一个失败（钱凭空消失或变多）
>

---

#### 一、什么是事务（Transaction）？

**定义**：事务是数据库中**一组逻辑相关的 SQL 操作**，这些操作**要么全部成功提交，要么全部失败回滚**，是一个不可分割的工作单元

**语法**：
```sql
BEGIN;          -- 开启事务（也叫 START TRANSACTION）
UPDATE account SET money = money - 100 WHERE id = 1;   -- 张三 -100
UPDATE account SET money = money + 100 WHERE id = 2;   -- 李四 +100
COMMIT;         -- 提交事务：上面两句都生效
-- 如果中间出错 → ROLLBACK; 回滚事务：上面两句都撤销，就像没执行过
```

> 重要前提：MySQL 的 **MyISAM 引擎不支持事务**，只有 **InnoDB 引擎**（MySQL 5.5+ 默认引擎）支持事务

---

#### 二、ACID 四大特性

| 字母 | 英文名 | 中文 | 核心一句话解释 | 银行转账例子对应 |
|---|---|---|---|---|
| **A** | **A**tomicity | **原子性** | 事务是最小执行单元，**要么全成功，要么全失败回滚**，不能只执行一半 | 转账中途服务器崩了，不能出现张三钱扣了李四没收到的情况 → 要自动回滚到转账前 |
| **C** | **C**onsistency | **一致性** | 事务执行**前后**，数据库都处于"业务规则正确"的状态 | 转账前总金额=张三500+李四500=1000；转账后=张三400+李四600=1000，总金额守恒，不能凭空产生或消失 |
| **I** | **I**solation | **隔离性** | 多个事务并发执行时，**一个事务内部的操作对其他事务是不可见的**，互不干扰 | 张三转账给李四的同时，王五也给张三转账，不能出现错乱（见下面"隔离级别"） |
| **D** | **D**urability | **持久性** | 事务一旦 **COMMIT 提交成功**，对数据的修改就是**永久的**，即使服务器断电/重启也不会丢失 | 转账成功后银行告诉你"转账完成"，哪怕下一秒银行机房断电，这笔记录也不会被撤销了 |

> ACID 分别是怎么实现的？（底层原理）**
>
> | 特性 | MySQL InnoDB 是怎么做到的？ |
> |---|---|
> | **A 原子性** | 靠 **undo log（回滚日志）**。每一步修改前都先写 undo log 记录"反向操作"，失败时用 undo log 把已经执行的步骤还原回去。 |
> | **C 一致性** | 这是事务的**最终目标**，其他三个特性都是为了保证一致性。实现靠的是A+I+D（数据库机制） + 业务代码自己校验（比如余额不能为负就写个CHECK约束或业务判断） |
> | **I 隔离性** | 靠 **MVCC（多版本并发控制）** + **锁**（行锁/间隙锁）以及4种隔离级别配置来实现|
> | **D 持久性** | 靠 **redo log（重做日志）**。修改时先写 redo log（WAL 预写式日志），即使数据页还没刷到硬盘，崩溃重启时可以从 redo log 恢复已提交的修改 |
> 回滚和重做就很像git的工作原理

---

#### 三、事务的4种隔离级别

> 为什么需要隔离级别？不能直接全部隔离了吗？
> 因为"完全隔离"（串行化）性能太差了，实际生产中要在"正确性"和"并发性能"之间做权衡
> 所以设计了4个等级，等级越高越正确，性能越差
**可能出现的3种并发问题（从严重到轻微）**：

| 问题 | 理解（两个事务 A、B 并发） | 例子 |
|---|---|---|
| **脏读**（最严重） | A **读到了 B 还没 COMMIT 的修改** → 结果 B 回滚了，A 白读了垃圾数据 | A 查账户余额看到 B 刚转进来 100（但 B 还没提交），A 正准备发货，B 回滚了，那 100 没了 |
| **不可重复读** | A 事务内**两次读同一行数据，结果不一样**（中间被 B 提交的修改改了） | A 事务先查余额=500 → B 提交+100 → A 再查余额=600，A自己事务还没结束，同一行却变了 |
| **幻读** | A 事务内**两次按同样条件查"行数集合"，行数不一样**（中间 B 提交了 INSERT/DELETE 新行） | A 事务先查"余额>500的用户" → 返回 10 行 → B 插入一个余额=600的新用户并提交 → A 再查同样条件 → 返回 11 行，像出现了幻觉一样 |

**4 种隔离级别对比（从低到高，MySQL 默认是 RR）**：

| 隔离级别（级别从低→高） | 脏读？ | 不可重复读？ | 幻读？ | 性能 |
|---|---|---|---|---|
| **读未提交**（Read Uncommitted） | 会发生 | 会 | 会 | 最好（几乎没用过） |
| **读已提交**（Read Committed, RC） |  解决了 | 会 | 会 | 较好（Oracle、SQL Server 默认） |
| **可重复读**（Repeatable Read, **RR**） |  解决了 |  解决了 | InnoDB 的 RR 通过 MVCC+间隙锁**基本解决了** | 一般（**MySQL 默认级别**，生产常用） |
| **串行化**（Serializable） |  解决了 |  解决了 | 完全解决 | 最差（所有事务排队执行，基本不用） |

---

### 1.4 简述索引的作用。

> 索引是数据库里最重要的性能优化手段（没有之一）
> **索引近似于书的目录**，不看目录找内容要翻整本书（全表扫描），看目录翻几页就找到了

---

#### 一、索引是什么？

**定义**：索引是数据库引擎为了**加快查询速度**，为表的一列（或多列）建立的一种**辅助数据结构**

> e.g:新华字典按"拼音"编排的拼音表、按"部首"编排的部首检字表，都是"索引"

**MySQL InnoDB 默认用的底层结构**：**B+ 树**（B-Plus Tree，多路平衡搜索树）

> 为什么选 B+ 树，而不是二叉搜索树、红黑树、B 树、哈希表？
> - 二叉搜索树/红黑树：一个节点只有两个子节点，树的高度太高（100万条数据约20层高）→ 磁盘IO次数多（每次换节点就是一次磁盘随机读，很贵！）
> - Hash 表：等值查询快 O(1)，但**不支持范围查询**（`WHERE age > 20` 这种就无了），也不支持排序
> - B 树：多路，高度低，但**每个节点存数据** 导致一页能装的节点数少，相比 B+ 树高度还是高
> - **B+ 树最优**：① 多路（几千路），高度通常 3~4 层就能装千万级数据；② 只有叶子节点存数据，非叶子节点只存索引键值 → 一页装更多节点 → 更矮 → 更少磁盘IO；③ 叶子节点用双向链表串起来 → 范围查询/排序超级快

---

#### 二、索引的作用

| 好处 | 具体说明 + 例子 |
|---|---|
| **① 大大加快查询速度（最主要）** | `SELECT * FROM student WHERE sno = 'S101'`<br>不加索引就要逐行扫描 100 万行；加了索引B+ 树查 3~4 次磁盘定位到行 |
| **② 加速排序和分组** | `ORDER BY create_time` 或 `GROUP BY status`<br>如果索引列顺序和 ORDER BY 一致，直接按索引顺序读，不需要内存/外排序（避免 `Using filesort`） |
| **③ 加速表之间的 JOIN 连接** | `FROM sc JOIN student ON sc.sno = student.sno`<br>如果 student.sno 是主键索引（默认就是），那每次匹配都能通过索引秒级定位 |
| **④ 保证唯一约束（唯一索引）** | 用户名、手机号、邮箱这种要求**不能重复**的字段就可以建 UNIQUE 唯一索引，数据库层面自动拒绝重复值 |

---

#### 三、索引的代价

| 代价 | 具体说明 |
|---|---|
| **① 占用额外存储空间** | 索引也是数据，一张表建 10 个索引，索引占用的磁盘空间可能比实际数据还大 |
| **② 降低 INSERT/UPDATE/DELETE 速度** | 每次增删改，不仅要改数据表，还要维护对应所有索引的 B+ 树结构（分裂、合并、平衡），写入性能下降<br>所以**写密集型（日志、订单）表，索引要控制数量** |
| **③ 不合理的索引会被优化器忽略** | 建了却用不上，白白浪费了上面两条成本（见"索引失效"常见情况） |

> **索引要按需建立，宁少勿滥**
> 一张表通常 5~10 个索引比较合理，超过 15 个就要反思是不是自己有问题


---

#### 四、索引失效的常见场景

> 总以为加了索引 SQL 就会快，但实际上 MySQL 优化器评估后**不走索引**，还是全表扫描，速度依然很慢，建索引建了个寂寞
> 用 `EXPLAIN` 看 SQL 的执行计划，`type` 列 = `ALL` 就是走了全表扫描，索引失效了

| 导致失效的写法 | 反例（索引是 `idx_sname(sname)`） | 为什么？ |
|---|---|---|
| ① **对索引列做函数/运算/类型转换** | `WHERE YEAR(create_time) = 2024`、`WHERE age + 1 = 20`、`WHERE sno = 101`（sno是字符串，你用数字去比） | B+ 树存的是"原始值"，你把列改了，索引里的排序就没用了 |
| ② **LIKE 模糊匹配以 % 开头** | `WHERE sname LIKE '%小%'`、`LIKE '%小'` | B+ 树按前缀有序，你把前缀遮住就没法按目录查（**后缀%是可以的**：`LIKE '张%'` 走索引） |
| ③ **联合索引不符合最左前缀原则** | 索引是 `(dept, sname)`，你写 `WHERE sname = '张三'`（跳过了 dept） | 联合索引的 B+ 树是先按第一列排，再按第二列排；跳过第一列，第二列是无序的，用不了 |
| ④ **WHERE 中用 OR 连接，且有一侧无索引** | `WHERE sname = '张三' OR phone = '13800138000'`（phone 没索引） | OR 只要一边没索引，MySQL 觉得全表扫反而比"先查索引再回表再合并"更划算 |
| ⑤ **NOT / != / <>**（部分情况） | `WHERE sname != '张三'` | MySQL 觉得排除结果可能太多，不如全表扫描一遍省事（如果数据分布很不均匀才可能走） |
| ⑥ **IS NOT NULL**（部分情况） | `WHERE sname IS NOT NULL` | 和上面同理，如果大多数行都 NOT NULL，优化器选择全表扫 |

---

#### 六、什么字段该建索引 / 不该建？

| 适合建索引的字段 | 不适合建索引的字段 |
|---|---|
| 主键（自动建） | 更新/插入频繁，查询很少的字段（写入维护代价 > 查询收益） |
| 经常出现在 `WHERE` 中的字段 | 区分度（基数）很低的字段：性别（男/女）、状态（0/1）→ 每次过滤一半，还不如全表扫 |
| 经常用于 `ORDER BY` / `GROUP BY` 的字段 | 允许 NULL 值太多的列（NULL 多了索引意义不大，虽然 InnoDB 现在支持了，但还是少用） |
| 经常用于 `JOIN ... ON ...` 的字段（外键列） | 内容很长的大字段：text/blob/varchar(1000) → 索引体积太大 |
| 值唯一（或非常接近唯一）的字段：手机号、邮箱 | 数据量太小的表（比如只有几百行的字典表）→ 全表扫比读索引还快 |

---

## 2. SQL学习笔记



### 2.1 SQL的分类

| 分类缩写 | 英文全称 | 中文名 | 常见命令 & 作用 |
|---|---|---|---|
| **DDL** | Data Definition Language | **数据定义语言** | `CREATE`（建库/建表） `ALTER`（改表结构） `DROP`（删库/删表） |
| **DML** | Data Manipulation Language | **数据操作语言** | `INSERT`（增） `UPDATE`（改） `DELETE`（删） |
| **DQL** | Data Query Language | **数据查询语言** | `SELECT`（查） |
| **DCL** | Data Control Language | 数据控制语言 | `GRANT`（授权） `REVOKE`（撤权）—— 一般 DBA 用，开发很少用 |
| **TCL** | Transaction Control Language | **事务控制语言** | `BEGIN` `COMMIT` `ROLLBACK` `SAVEPOINT` |
| **DAL** | Data Admin Language | 数据管理语言 | `SHOW` `EXPLAIN` `ANALYZE` —— 调试/分析用 |

> 一般只需要学会DDL + DML + DQL，这是最基本的用法

---

### 2.2 SQL 语法规定

| # | 规定 | 示例 / 说明 |
|---|---|---|
| 1 | **每条语句必须以分号 `;` 结尾** | 就像 Java 每句末尾分号一样 |
| 2 | **SQL 关键字不区分大小写** | `SELECT` / `select` / `Select` 完全一样，一般约定俗成**关键字大写，自己的表名列名小写**，这样可读性好 |
| 3 | **字符串和日期必须用单引号 `'` 包起来** | `'张三'` `'2024-01-01'` 正确；`"张三"`（双引号在MySQL里表示"标识符"如字段名） |

#### 注释的 3 种写法

| 写法 | 类型 | 说明 |
|---|---|---|
| `-- 空格` + 注释内容 | 单行注释 | 最常用 |
| `#` + 注释内容 | 单行注释 | MySQL 特有，Oracle / PostgreSQL 不支持 |
| `/* 多行内容 */` | 多行注释 | 和Java的 `/* */` 一样，跨多段屏蔽代码 |

---

### 2.3 MySQL 常用数据类型

> 和Java 写变量前要声明类型一样（`int a` / `String s`），SQL建表时每一列也必须先声明是什么类型

| 分类 | 类型 & 用法 | 典型使用场景 |
|---|---|---|
| **整数** | `TINYINT` (-128~127) 1 字节<br>`INT` (-21亿~21亿) **4 字节 最常用**<br>`BIGINT` (-922亿亿) 8 字节 | TINYINT：年龄、状态（0/1）<br>INT：自增主键 id、数量<br>BIGINT：超大主键 |
| **小数** | `FLOAT` / `DOUBLE` 不精确（有浮点误差）<br>`DECIMAL(M,D)` **高精度、无误差**<br>M=总位数, D=小数位 | 金额、学分、成绩必须用 `DECIMAL(10,2)` |
| **字符串** | `CHAR(N)` 固定长度 N（最多 255）<br>`VARCHAR(N)` 变长、最多 N 字符 **最常用**<br>`TEXT` 最多 65535 字符 | CHAR：手机号(11位)、身份证、学号（定长，不浪费）<br>VARCHAR：姓名、地址、标题（变长，空间利用率高）<br>TEXT：文章正文、长文本评论 |
| **日期时间** | `DATE` 'YYYY-MM-DD'<br>`TIME` 'HH:MM:SS'<br>`DATETIME` 'YYYY-MM-DD HH:MM:SS' **最常用**<br>`TIMESTAMP` 同 DATETIME，但范围小，带时区转换 | DATE：生日、入职日期<br>DATETIME：创建时间、订单时间、支付时间<br>TIMESTAMP：`ON UPDATE CURRENT_TIMESTAMP` 能更新时间戳（更新数据自动变） |
| **枚举 / 布尔** | `ENUM('男','女')` 只能选一项<br>`BOOL / BOOLEAN` 本质是 `TINYINT(1)`，0=假，非0=真 | ENUM：性别、订单状态、审核状态<br>BOOL：是否删除（软删除标记）、是否启用 |


### 2.4 DDL 数据定义语言（CREATE / ALTER / DROP）

#### 一、数据库级别的 DDL（先有库，才能建表）

```sql
CREATE DATABASE 库名;                                  -- 新建数据库
DROP   DATABASE 库名;                                  -- 删除数据库（无法撤销）
SHOW   DATABASES;                                      -- 查看服务器上所有库
USE    库名;                                           -- 切换当前要操作的库（像 cd 进目录）
```

**示例**：
```sql
CREATE DATABASE zeqi_demo DEFAULT CHARSET utf8mb4;    -- 建库+指定字符集（防中文乱码）
USE zeqi_demo;
SELECT DATABASE();                                     -- 查看当前在哪个库
DROP DATABASE zeqi_demo;                               -- 练习完可以删掉
```
---
#### 二、表级别的 DDL（先有库，才能建表）
| 命令 | 作用 | 示例 |
|---|---|---|
| `CREATE TABLE` | 新建表 | `CREATE TABLE 表名 (列名 数据类型, ...);` |
| `ALTER TABLE` | 修改表 | `ALTER TABLE 表名 ADD/DROP/MODIFY 列名 数据类型, ...;` |
| `DROP TABLE` | 删除表 | `DROP TABLE 表名;` | |


---

#### 三、建表时约束类型（建表时写在列定义后面的"修饰符"）

约束 = 数据库层面强制限制数据，保证数据质量

| 约束关键字 | 作用 | 示例 |
|---|---|---|
| `NOT NULL` | **非空**：该列不能为 NULL（空值） | `sname VARCHAR(50) NOT NULL` |
| `DEFAULT 值` | **默认值**：不填该列就用默认值 | `create_time DATETIME DEFAULT NOW()` |
| `UNIQUE` | **唯一**：全表该列不能有重复值（允许 NULL） | `phone CHAR(11) UNIQUE` |
| `PRIMARY KEY` | **主键** = NOT NULL + UNIQUE 二合一，一张表只能 1 个 | `sno VARCHAR(20) PRIMARY KEY` |
| `AUTO_INCREMENT` | **自增**：整数主键自动 +1（必须配合主键用） | `id INT PRIMARY KEY AUTO_INCREMENT` |
| `FOREIGN KEY` | **外键**：本表某列引用另一个表的主键，保证引用一致性 | `FOREIGN KEY (sno) REFERENCES student(sno)` |
| `CHECK (条件)` | **校验**：插入/修改前先检查条件，不满足就拒绝 | `CHECK (grade BETWEEN 0 AND 100)`<br>MySQL 8.0.16+ 才真正生效 |

---

### 2.6 DML 数据操作语言（INSERT / UPDATE / DELETE）

> DML 是对"表里面的行数据"做增、改、删操作，**不碰表结构**。

#### 一、INSERT 插入数据

```sql
-- 写法 1 最规范，推荐（指定要插入的列名）
INSERT INTO student (sno, sname, sdept) VALUES ('S999', '测试学生', '计算机学院');
-- 优点：列顺序随便换、加新列后旧代码不用改

-- 写法 2  （省略列名，必须和表定义顺序完全一致）
INSERT INTO student VALUES ('S998', '测试2', '软件学院');

-- 写法 3 一次 INSERT 插入多行（性能好，减少和数据库交互次数）
INSERT INTO student (sno, sname, sdept)
VALUES ('S001', 'A', '数学院'),
       ('S002', 'B', '物理院'),
       ('S003', 'C', '外语院');
```

**INSERT 易错点**：
- 主键重复：`1062 Duplicate entry` 报错 → 解决：先 `DELETE` 旧的，或用 `INSERT ... ON DUPLICATE KEY UPDATE`
- 外键约束：插 sc 之前 student/course 必须先有对应 sno/cno（先建学生才能选课）


---

#### 二、UPDATE 修改数据

**语法**：`UPDATE 表 SET 列1=新值1, 列2=新值2 WHERE 条件;`

**示例**：把 S101 学生改名叫"小电"，学院改到"软件学院"
```sql
UPDATE student SET sname = '小电', sdept = '软件学院' WHERE sno = 'S101';
```

**UPDATE 定律**：
1. **必须先写 WHERE！** 不写 WHERE = 全表所有行都被改（把 1 万学生名字全改成小电）
2. 改之前最好先 `SELECT * ... WHERE 同条件` 查一下到底会影响多少行，心里有数
3. 生产环境建议开事务：`BEGIN; UPDATE...;` 检查影响行数对再 `COMMIT;`，不对就 `ROLLBACK;`

---

#### 三、DELETE 删除数据

**语法**：`DELETE FROM 表 WHERE 条件;`

**示例**：删掉 sno=S999 的学生
```sql
DELETE FROM student WHERE sno = 'S999';
```

##### DELETE vs TRUNCATE vs DROP 区别

| 命令 | 删除对象 | 能加 WHERE 吗？ | 能回滚吗？ | 速度 & 原理 |
|---|---|---|---|---|
| `DELETE` | 表的行数据（表结构保留） | 可以按条件删部分行 | 事务内执行可以 ROLLBACK | 慢，逐行删，写 redo/undo log；**自增 ID 计数不会重置** |
| `TRUNCATE` | 表的全部行数据（表结构保留） | 不可以，一删就是整表空 | 不能回滚（DDL 操作） | 快，直接销毁整表然后重建空表；**自增 ID 重置为 1** |
| `DROP` | 整个表（结构 + 所有数据全消失） | 不可以 | 不能回滚 | 最快，直接删掉表文件（.ibd / .frm） |

---

### 2.7 DQL 数据查询语言（SELECT）

#### 一、SELECT 完整语法骨架

```
SELECT [DISTINCT] 列1, 列2, 聚合函数...      -- ⑥ 要返回什么列？去不去重？
  FROM 表1                                      -- ① 从哪张表开始？
  [LEFT/RIGHT/INNER] JOIN 表2 ON 表1.a=表2.a  -- ② 还要连接哪张表？条件？
 WHERE 行级筛选条件                           -- ③ 哪些行要？分组前筛
 GROUP BY 分组列1, 分组列2...                -- ④ 按哪些列分小组？
HAVING 组级筛选条件                           -- ⑤ 哪些小组要？分组后筛
 ORDER BY 排序列1 DESC, 列2 ASC              -- ⑦ 结果怎么排序？
 LIMIT [跳过N条,] 取几条;                     -- ⑧ 分页/只取前几条？
```

---


#### 二、WHERE 子句里的 10 种运算符（行级筛选，最常用）

| 类型 | 运算符 | 示例 & 说明 |
|---|---|---|
| 比较 | `=` `>` `<` `>=` `<=` `!=` `<>` | `grade >= 60`，`sdept != '数学学院'` |
| 范围 | `BETWEEN a AND b` | `grade BETWEEN 60 AND 90` = **闭区间** [60,90]，两边都包含（容易误以为左闭右开） |
| 集合 | `IN (v1, v2, v3)` | `sno IN ('S101','S102')` —— 简化一堆 OR 的写法 |
| 模糊 | `LIKE 模式`（`%`=任意多字符, `_`=恰好1字符） | `sname LIKE '张%'` 姓张；`sname LIKE '李_'` 姓李+两个字<br>`'%小%'` 是"前后都有 %"，会让 B+ 树索引失效，变成全表扫描|
| 空判断 | `IS NULL` / `IS NOT NULL` | `grade IS NULL` 缺考<br>**不能写 `grade = NULL`** NULL 参与任何比较结果都是 UNKNOWN，永远假，查不出任何行 |
| 逻辑 | `AND` / `OR` / `NOT` | `sdept='计院' AND grade>=80`；`NOT (a AND b)` = `NOT a OR NOT b` |

**NULL 的坑**：`WHERE grade != 80` 查不到 grade 为 NULL 的学生（NULL ≠ 80 的结果 = UNKNOWN，被 WHERE 过滤）
**要包含 NULL 必须写**：`grade != 80 OR grade IS NULL`

---

#### 三、聚合函数（和GROUP BY一起用，返回 1 个汇总值）

| 函数 | 作用 & 注意事项 |
|---|---|
| `COUNT(*)` | 统计行数（哪怕整行全是 NULL 也算）—— 常用"查总数" |
| `COUNT(列名)` | 统计该列非 NULL 的行数（列里有 NULL 的行被忽略） |
| `COUNT(DISTINCT 列)` | **去重计数**：统计该列有多少个不同的非空值 |
| `SUM(列)` | 求和（只能数值列，NULL 当 0 不参与） |
| `AVG(列)` | 求平均（**自动忽略 NULL** —— 缺考学生不参与平均分，符合常理） |
| `MAX(列)` / `MIN(列)` | 最大 / 最小值（日期列也能用：MAX(时间) = 最晚时间） |


---

#### 四、JOIN 表连接

> **表连接本质**：把两张表 A（左）和 B（右）按某个关联条件，拼成一张更宽的宽表。
> 示例：student(sno, sname) + sc(sno, cno, grade) 才能同时看到"学生姓名和他的成绩"

| 连接类型 | 含义（Venn 图描述） | 典型场景 & 举例 |
|---|---|---|
| **1. INNER JOIN  最常用** |  **交集**：只保留两边 ON 条件**都匹配**的行 | 查"已有成绩的学生姓名+成绩"：INNER JOIN 自动忽略没选课的学生 |
| **2. LEFT [OUTER] JOIN** |  **A 全集**：左表 A 每一行都保留；B 能匹配就拼数据，匹配不上的 B 列全填 NULL | 查"所有学生（含没选课的）的成绩"：必须用 LEFT JOIN，否则 S110 这种没选课的人会消失 |
| **3. RIGHT [OUTER] JOIN** |  **B 全集**：和 LEFT 反过来，右表全保留（用得少，全改成 LEFT JOIN 就行） | 很少用，交换两个表的顺序改写成 LEFT JOIN 更易读 |
| **4. FULL [OUTER] JOIN** |  **A ∪ B 并集**：两边行全保留，缺的补 NULL |  **MySQL 不支持 FULL JOIN！** 只能用 `LEFT JOIN ... UNION RIGHT JOIN ...` 模拟 |
| **5. LEFT JOIN + IS NULL** |  **A - B 差集**：找"在 A 中存在但在 B 里完全匹配不上"的行 | 本文件查询题 9：找"没选课的学生" = `student LEFT JOIN sc WHERE sc.sno IS NULL` |
| **6. RIGHT JOIN + IS NULL** |  **B - A 差集**：反向差集（同理，一般改成 LEFT JOIN...IS NULL） | 没人选的课程名 |
| **7. CROSS JOIN** |  **笛卡尔积**：A 的每行 × B 的每行，结果行数 = A行数 × B行数 | 9一般都是**忘记写 ON 条件**的Bug，不要这样干 |


- 三表连接写法：`FROM a JOIN b ON a.x=b.x JOIN c ON b.y=c.y`（每个 JOIN 紧配一个 ON）
- **左连接后，对右表的筛选条件写在了 WHERE 里 就自动退化成内连接**

---

#### 六、子查询（嵌套查询）—— 一个 SELECT 里再套一个 SELECT

"一步查不出来"的时候就用子查询，按**子查询出现在 SQL 的什么位置**分 3 类：

| 分类 | 位置 & 原理 | 示例 |
|---|---|---|
| **① WHERE 型子查询** **最常用** | 子查询结果当 WHERE 的条件值 / 值列表 | 查"最高分的学生姓名"：<br>`SELECT sname FROM student WHERE sno = (SELECT sno FROM sc ORDER BY grade DESC LIMIT 1)` |
| **② FROM 型子查询** | 子查询结果"假装是一张临时表" → **必须取别名 AS t** | 查"每个学生的平均分的平均值"：<br>`SELECT AVG(t.sa) FROM (SELECT sno, AVG(grade) sa FROM sc GROUP BY sno) AS t` |

---

#### 七、ORDER BY 排序 + LIMIT 分页

```sql
-- 先按列1降序，列1值相同再按列2升序排（每一列单独写 ASC（默认升序）/DESC（降序））
ORDER BY col1 DESC, col2 ASC

-- LIMIT 两种写法：
LIMIT 10;                     -- 取前 10 行（TOP 10）
LIMIT 20, 10;                 -- 跳过 20 行再取 10 行
LIMIT 10 OFFSET 20;           -- 同上
```

---

