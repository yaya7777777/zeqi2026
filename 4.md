# Part 2 · 项目实战：用户管理系统（Java Spring Boot 版 · 含完整 Postman 接口测试教程）

> 📚 配套代码目录：`../user-management/`
> 📚 关联文档：前面完成的 `2.md`（HTTP 基础 + RESTful API 设计）、`3.md`（数据库 + SQL 教程）
> 🧪 配套测试工具：**Postman**（官网下载免费版 https://www.postman.com/downloads/ ）
> 🎯 技术栈（Java 主栈版 · 本项目实际采用）：**Spring Boot 2.7 + Spring Web MVC + Spring JDBC Template + H2 嵌入式数据库 + JSR-303 参数校验 + Lombok**

---

## 目录
1. [简答题（2 道，详细讲解）](#1-简答题2-道详细讲解)
2. [项目架构总览（3 层架构 + Maven 目录）](#2-项目架构总览3-层架构-maven-目录)
3. [🔥 核心 Java 代码从 0 到 1 讲解（10 个类逐个拆解）](#3--核心-java-代码从-0-到-1-讲解10-个类逐个拆解)
4. [数据库 & 配置文件详解（pom.xml / application.yml / schema.sql）](#4-数据库--配置文件详解pomxml-applicationyml-schemasql)
5. [🔥 Postman 接口测试 · 手把手图文教程（重点！共 7 个接口+异常场景）](#-postman-接口测试--手把手图文教程重点共-7-个接口异常场景)
6. [最终运行 & 提交检查清单](#6-最终运行--提交检查清单)

---

---

## 1. 简答题（2 道，详细讲解）

### ① 请描述一次"用户注册"的完整流程：从客户端发起 HTTP 请求，到数据写入数据库并返回响应，中间经历了哪些环节？

> 💡 **学习提示**：这是后端面试**超高频 100% 考题**，面试官通过这道题看你对"Web 请求全链路"有没有宏观认识。把下面 8 步**背熟**，再加一个自己画的时序图，面试稳过。

#### 「用户注册」8 步完整流程（Java Spring Boot 版）

```
  📱 客户端(浏览器/Postman)          🌐 网络层             🚀 后端(Spring Boot)              💾 H2/MySQL
        │                              │                        │                               │
        │ 1. 组装 HTTP 请求           │                        │                               │
        │   POST /api/auth/register  │                        │                               │
        │   Content-Type: JSON       │                        │                               │
        │   Body:{username,password} │                        │                               │
        │ ───────────────────────────────────────────────────────────────────────────────────────►│
        │                              │ 2. TCP 三次握手         │                               │
        │                              │    + TLS(HTTPS) 握手   │                               │
        │                              │ ──────────────────────►│                               │
        │                              │                        │ 3. Tomcat 解析 HTTP          │
        │                              │                        │    (Spring Boot 内嵌 Tomcat) │
        │                              │                        │    → 转成 HttpServletRequest │
        │                              │                        │                               │
        │                              │                        │ 4. Filter / Interceptor 链  │
        │                              │                        │    CORS → AuthInterceptor    │
        │                              │                        │    (这题注册=放行)            │
        │                              │                        │                               │
        │                              │                        │ 5. DispatcherServlet 分发   │
        │                              │                        │    → @PostMapping 命中       │
        │                              │                        │    → @Valid 参数校验(400?)   │
        │                              │                        │                               │
        │                              │                        │ 6. Controller → Service     │
        │                              │                        │    Service:①判空 ②查重(409?) │
        │                              │                        │ ────────────────────────────►│
        │                              │                        │ 7. Repository (JdbcTemplate)│
        │                              │                        │    INSERT INTO users(?,?) 预编译
        │                              │                        │                               │ 8. DB 执行 SQL
        │                              │                        │                               │   · ACID 事务 BEGIN/COMMIT
        │                              │                        │ ◄────────────────────────────│
        │                              │                        │ 9. GlobalException? 否      │
        │ ◄───────────────────────────────────────────────────────────────────────────────────────│
        │                              │                        │    ResponseEntity(201 CREATED)
        │ 10. 前端拿到 201 → 跳登录页 │                        │    + UserVO(无 password)
```

#### 面试标准答案（分 10 小点背）：

| 步骤 | 环节 | 要点 & 面试关键词 |
|---|---|---|
| **1** | **客户端组装请求** | HTTP=`POST`(RESTful 创建语义)、URL=`/api/auth/register`、`Content-Type: application/json`、Jackson 序列化 JSON |
| **2** | **网络传输** | DNS→IP；TCP 三次握手；HTTPS=TLS1.3；路由器/Nginx 负载均衡(多机部署) |
| **3** | **Servlet 容器解析** | Spring Boot **内嵌 Tomcat**（默认）监听 5000，把字节流转 `HttpServletRequest`（Flask=Werkzeug；SSM=独立 Tomcat war 部署） |
| **4** | **过滤器链 & 拦截器** | `Filter`(Servlet 规范级) → `HandlerInterceptor`(Spring MVC 级)：本项目 `WebMvcConfig.addCorsMappings` 跨域 → `AuthInterceptor`（注册接口在 excludePathPatterns=放行） |
| **5** | **DispatcherServlet 分发** | Spring MVC 核心：根据 URL+Method 找 `HandlerMethod`（@PostMapping）→ `HandlerAdapter` 调用 Controller |
| **6** | **Controller + 参数校验** | `@RequestBody RegisterDTO` Jackson 反序列化；`@Valid` 触发 `@NotBlank/@Size` 校验 → 失败抛 `MethodArgumentNotValidException` → 全局异常 `400` |
| **7** | **Service 业务层** | ① 非空/Trim ② `findByUsername` 查重 → 冲突抛 `BusinessException(409 Conflict)` |
| **8** | **Repository 数据层** | `JdbcTemplate.update(PreparedStatementCreator, KeyHolder)` → **预编译 `?` 占位符防 SQL 注入**（绝对不能字符串拼接！）→ `KeyHolder` 拿回自增主键 id |
| **9** | **DB 持久化** | H2/MySQL 执行：解析器→优化器→执行器→InnoDB/MVStore：Buffer Pool 写脏页→Redo Log(WAL 崩溃安全)→Binlog(主从)→COMMIT；`@Transactional` 切面自动 BEGIN/COMMIT |
| **10** | **返回响应** | Controller `ResponseEntity.status(CREATED).body(Result<UserVO>)` → Jackson 序列化，**UserVO 手写白名单 password 不出现在 JSON**（安全红线）→ 前端渲染 |

> ⚠️ **3 个灵魂追问（面试官 90% 概率接着问）**：
> - Q：为什么用预编译 `?` 防 SQL 注入？A：SQL 结构和参数分离，即使参数里有 `' OR 1=1 --`，也会被当成"字符串值"而不是 SQL 语法执行；`PreparedStatement` 还会缓存执行计划（同 SQL 多次执行更快）。
> - Q：为什么返回 201 而不是 200？A：RESTful 语义：**201 CREATED 表示"服务器成功创建了新资源"**；统一约定比通用 200 更精准，API 自文档化。
> - Q：为什么 `UserVO` 里没有 password 字段？A：敏感字段严禁序列化到响应（即使加密也不该返回）；Chrome DevTools / Fiddler 抓包 / 中间人攻击都能看到。**写 DTO 白名单 = 招新考试加分点**。

---

### ② 为什么用户数据要持久化到数据库，而不是只存在程序内存中？

> 💡 **考点**：看你是否理解「**持久化 Persistence**」的本质意义（内存 vs 磁盘 8 大对比维度）。

#### 一句话核心答案：
> **内存是"易失性存储"，断电/重启即丢失；数据库是"持久化存储"，写入后即使断电重启数据永远存在。**
> 但面试只说这一句不够，要展开成下面 7 个维度（每个维度各举 1 个例子就满分）：

| 维度 | 内存（Java Heap / `HashMap`） | 数据库（H2 / MySQL / SQLite） | 举个实际反例 ❌（只用内存会怎样？） |
|---|---|---|---|
| **① 持久性** | ❌ 易失：`kill -9` / 重启 / OOM 立即清空 | ✅ 持久：H2 写 `./data/usersdb.mv.db` 磁盘文件；MySQL 写 `.ibd` 表空间 | 凌晨运维重启了下机器 → 早上老板发现**所有注册用户全没了**，公司直接倒闭 |
| **② 容量** | ❌ 极小：JVM `-Xmx` 一般 4G~8G，还要 GC 开销；`HashMap` 存 1000 万对象 = OOM | ✅ 无限：磁盘几 TB；分库分表后能存 PB 级（百亿行） | 你用 `ConcurrentHashMap<Long,User>` 存 1000 万用户 → JVM `OutOfMemoryError: Java heap space` 直接崩溃 |
| **③ 并发安全** | ❌ 手动加锁：多线程同时改同一条（扣余额），自己写 `synchronized` / `ReentrantLock` / `Atomic`，写错就超卖 | ✅ 原生 ACID + MVCC 行锁：`UPDATE account SET balance=balance-100 WHERE id=1 AND balance>=100` 并发安全 | 双 11 秒杀 1000 人抢同一件商品 → 内存版 HashMap 没锁 → **超卖 2000 份**，老板亏哭 |
| **④ 检索能力** | ❌ 全表扫：按 username LIKE 模糊搜索？你得遍历 Map 一个个 `contains()`，O(N) 卡死 | ✅ B+ 树索引：`SELECT * FROM users WHERE username LIKE '张%'` 走 username 唯一索引，O(logN) 毫秒级 | 做后台用户搜索，输入"张"想找姓张的 → 内存写不出高效索引，页面卡 30s 超时 |
| **⑤ 崩溃恢复** | ❌ 0 恢复：写到一半进程崩溃？刚才那 50% 成功 50% 失败的数据全乱套（比如钱扣了订单没建） | ✅ Write-Ahead Log：Redo+Undo+Binlog；崩溃后自动回滚未提交事务；`@Transactional` 保证要么全成要么全败 | 用户支付：扣余额成功、创建订单成功，但**加积分前**JVM 崩溃重启 → 内存版：用户钱没了订单也没了，投诉爆炸 |
| **⑥ 多进程共享** | ❌ 进程隔离：A JVM 的 Heap，B JVM / Go 程序 / 管理后台完全读不到；共享内存 IPC 巨复杂 | ✅ 通用 JDBC 协议：任何语言（Java/Go/Python/Node）用 H2/MySQL 驱动 + 标准 SQL 都能读写同一份 `usersdb.mv.db` 文件 | 你用 `-server -XX:+UseG1GC` 起了 4 个 Spring Boot 实例（多核并发）→ 每个实例一份 `ConcurrentHashMap` session → 用户在 A 机登录被 LB 分配到 B 机就 **401 登出**（经典"分布式 Session 问题"= 招新加分点！解决：放 Redis） |
| **⑦ 生态工具** | ❌ 0 生态：自己写备份？做主从？做 CSV 导出？ | ✅ 生态完善：H2 自带 `/h2-console` Web 控制台；MySQL 有 `mysqldump` 每日备份+主从+binlog 闪回+DataGrip/Navicat 可视化 | DBA 手贱 `DROP TABLE users` → H2/MySQL 有 binlog2sql / 快照闪回；内存版只能跑路（跑路.gif） |

> 🎯 **面试总结公式（套话）**：
> *"程序内存是**计算资源**，用于**临时运算**；数据库是**存储资源**，用于**永久保存状态**。二者配合使用：热数据放 Redis 内存缓存加速，冷数据落 MySQL/H2 磁盘持久化，这就是经典的"内存 + 磁盘分层架构"。"*

---

---

## 2. 项目架构总览（3 层架构 + Maven 目录）

### 2.1 项目目录结构（Maven 标准工程 · 工业级规范）

```
zeqi2026/
└── user-management/                                     # 📁 项目根目录（cd 进这里执行 mvn 命令）
    │
    ├── pom.xml                                          # 📦 Maven POM（依赖/插件/JDK版本，= requirements.txt + build.gradle）
    │
    ├── data/                                            # 📀 H2 数据库文件自动生成目录（运行后出现）
    │   └── usersdb.mv.db                                #     H2 单个文件持久化，= SQLite 的 users.db
    │
    └── src/
        ├── main/
        │   ├── java/com/zeqi/usermanagement/            # 📂 Java 源码根（按功能分包=阿里Java手册推荐）
        │   │   ├── UserManagementApplication.java       # 🚀 启动类（@SpringBootApplication，Spring Boot 入口）
        │   │   ├── annotation/                          # 🔖 自定义注解包
        │   │   │   └── AuthRequired.java                #      @AuthRequired（贴方法/类=需要登录）
        │   │   ├── config/                              # ⚙️ 配置类包（Spring 配置）
        │   │   │   ├── WebMvcConfig.java                #      注册拦截器 + CORS 跨域
        │   │   │   └── GlobalExceptionHandler.java      #      全局异常处理器（统一 Result.fail 格式）
        │   │   ├── controller/                          # 🎮 控制层（Presentation）= 接 HTTP + 返回 JSON
        │   │   │   ├── AuthController.java              #      注册 + 登录（公开，不需要登录）
        │   │   │   └── UserController.java              #      查看自己/列表/改用户名/改密码/注销（需要 @AuthRequired）
        │   │   ├── dto/                                 # 📦 数据传输对象（和前端交互的 JSON 契约）
        │   │   │   ├── Result.java                      #      ✨ 统一响应 {code,message,data}
        │   │   │   ├── RegisterDTO.java                 #      注册请求 @NotBlank/@Size
        │   │   │   ├── LoginDTO.java                    #      登录请求
        │   │   │   ├── UserVO.java                      #      🔒 用户返回视图（白名单=无 password！）
        │   │   │   ├── LoginVO.java                     #      登录返回 = UserVO + sessionId
        │   │   │   └── AccountDTOs.java                 #      改用户名/改密码/注销请求体(内部静态类聚合)
        │   │   ├── entity/                              # 💾 实体层（和 DB 表一一对应）
        │   │   │   └── User.java                        #      users 表实体（@Data = Lombok 自动 Getter/Setter）
        │   │   ├── exception/                           # ⚠️ 异常定义
        │   │   │   └── BusinessException.java           #      业务异常（400/401/404/409 + message）
        │   │   ├── interceptor/                         # 🛡️ 拦截器
        │   │   │   └── AuthInterceptor.java             #      @AuthRequired 校验器（= Flask @login_required）
        │   │   ├── repository/                          # 🗃️ 数据访问层（DAO/Mapper）
        │   │   │   └── UserRepository.java              #      JdbcTemplate 手写 SQL（新手最容易理解 SQL 本身）
        │   │   └── service/                             # 🧠 业务逻辑层（核心 if/else 写这里）
        │   │       ├── UserService.java                 #      7 个业务方法 + @Transactional 事务
        │   │       └── SessionManager.java              #      🔐 内存 Session（ConcurrentHashMap<sessionId,userId>）
        │   │
        │   └── resources/                               # 📄 资源文件（classpath 根目录）
        │       ├── application.yml                      #      Spring Boot 核心配置（端口5000 / H2 URL / JdbcTemplate SQL日志）
        │       ├── schema.sql                           #      建表脚本（CREATE TABLE IF NOT EXISTS users...，启动自动执行）
        │       └── data.sql                             #      初始化测试数据（admin / admin123，MERGE INTO 避免重复插入）
        │
        └── test/java/                                   # 🧪 单元/集成测试（以后写 JUnit 5 用）
```

### 2.2 3 层分层架构（经典后端分层，面试必画！）

```
                     ┌─────────────────────────────────────────────────┐
                     │   🧪 客户端：Postman / 浏览器 / Apifox          │
                     │   (发起 HTTP 请求，带 Headers:sessionId + Body)  │
                     └───────────────────────┬─────────────────────────┘
                                             │
                      ┌──────────────────────▼──────────────────────┐
                      │  🚀 Spring Boot 内嵌 Tomcat (port 5000)      │
                      │  → 解析 HTTP 字节流 → HttpServletRequest    │
                      └──────────────────────┬──────────────────────┘
                                             │
                ┌────────────────────────────▼─────────────────────────────┐
                │  ① Presentation 表现层 (Controller) 【接 HTTP / 返 JSON】 │
                │  = AuthController / UserController                        │
                │  · @PostMapping @GetMapping 路由映射                      │
                │  · @Valid JSR-303 参数校验失败 → 400                      │
                │  · ResponseEntity 设置 HTTP Status（201/401/404/409）    │
                │  · 调用 Service 纯业务方法，绝不自己写 SQL                 │
                └────────────────────────────┬─────────────────────────────┘
                                             │
                ┌────────────────────────────▼─────────────────────────────┐
                │  ② Business Logic 业务层（Service）【所有 if/else 写这里】 │
                │  = UserService + SessionManager                           │
                │  ⭐ 核心层：参数二次校验 / UNIQUE 查重 / 密码比对 / 事务    │
                │  · @Transactional 声明式事务（写操作 BEGIN/COMMIT/ROLLBACK）│
                │  · 抛 BusinessException(409/401) → GlobalExceptionHandler  │
                └────────────────────────────┬─────────────────────────────┘
                                             │
                ┌────────────────────────────▼─────────────────────────────┐
                │  ③ Data Access 数据层（Repository）【纯 SQL 不写业务】      │
                │  = UserRepository 基于 JdbcTemplate                        │
                │  · `?` 占位符 + PreparedStatement = 防 SQL 注入            │
                │  · KeyHolder 拿回自增主键 ID                                │
                │  · BeanPropertyRowMapper 自动映射 ResultSet → User 实体    │
                └────────────────────────────┬─────────────────────────────┘
                                             │
                              ┌──────────────▼──────────────┐
                              │  💾 H2 嵌入式数据库          │
                              │  · 文件模式：./data/usersdb   │
                              │  · MODE=MySQL 高度兼容语法    │
                              │  · Web 控制台 /h2-console    │
                              └─────────────────────────────┘

 ╔═══════════════════════════════════════════════════════════════════════════════════════════════════╗
 ║ 🔐 🔺 旁路组件（AOP 切面思想，贯穿所有层）= 招新题"登录态"核心实现                                    ║
 ║   · @AuthRequired 注解 →贴在需要登录的 Controller 方法上                                           ║
 ║   · AuthInterceptor.preHandle() →读 Header:sessionId →查 ConcurrentHashMap SessionManager         ║
 ║   · 校验失败=直接写 401 JSON response，不进 Controller；成功=ThreadLocal 塞 userId → Controller 取 ║
 ╚═══════════════════════════════════════════════════════════════════════════════════════════════════╝
```

### 2.3 7 个 API 汇总表（和 Python 版 100% 一致，Postman 脚本完全复用！）

> 🔴 **超重要**：Java 版 URL / Method / Header / Body / 响应 JSON 字段和之前 Python Flask 版**一个字都没改**！
> 所以你之前写的 Postman Collection 完全不用动，只是启动命令从 `python app.py` 换成了 `mvn spring-boot:run`，端口都是 5000。

| # | API 功能 | HTTP 方法 | 完整 URL | 登录态 | 请求体 JSON 示例 | 成功返回 | 常见失败码 |
|---|---|---|---|---|---|---|---|
| **1** | 注册 | `POST` | `http://127.0.0.1:5000/api/auth/register` | ❌ | `{"username":"zhangsan","password":"123456"}` | **201** Created + User 对象（无 password） | 400 参数空 / 409 用户名重复 |
| **2** | 登录 | `POST` | `http://127.0.0.1:5000/api/auth/login` | ❌ | `{"username":"admin","password":"admin123"}` | **200** OK + LoginVO(含 sessionId) | 400 参数空 / 401 账号密码错 |
| **3** | 查看自己 | `GET` | `http://127.0.0.1:5000/api/users/me` | ✅ | 无 | **200** OK + 当前用户 | 401 session 无效 |
| **4** | 用户列表(加分) | `GET` | `http://127.0.0.1:5000/api/users` | ✅ | 无 | **200** OK + {total, users:[...]} | 401 未登录 |
| **5** | 改用户名(加分) | `PATCH` | `http://127.0.0.1:5000/api/users/username` | ✅ | `{"username":"zhangsan_new"}` | **200** OK + 更新后用户 | 401 / 409 新名冲突 |
| **6** | 改密码(加分) | `PATCH` | `http://127.0.0.1:5000/api/users/password` | ✅ | `{"oldPassword":"admin123","newPassword":"new456"}` | **200** OK + 提示请重新登录（踢所有设备下线） | 401 原密码错 / 400 新旧相同 |
| **7** | 注销账号(加分) | `DELETE` | `http://127.0.0.1:5000/api/users/me` | ✅ | `{"password":"admin123"}`（二次确认） | **200** OK + 提示注销成功 | 401 确认密码错 |

> 🔐 **登录态机制（招新题核心要求！）**
> - 登录成功：`SessionManager` 内部 `ConcurrentHashMap<String,Long>` 存 `Sessions.put(uuid, userId)`
> - 前端把 `sessionId` 存 `localStorage` / Cookie
> - 调 **3~7 需要登录**的接口，**必须在 HTTP Header 里加**：
>   ```
>   Key:   sessionId
>   Value: a1b2c3d4-5678-90ef-ghij-klmnopqrstuv   ← 登录返回的那串
>   ```
> - `AuthInterceptor` 拦截 → 查不到/为空 → 直接写 **401 Unauthorized**（符合题目明确要求 ✅）

---

---

## 3. 🔥 核心 Java 代码从 0 到 1 讲解（10 个类逐个拆解）

> 💡 因为 Java 是强类型+分层严格语言，我们写的类比 Python 一个 `app.py` 多，但**每个类的职责更单一、更符合工业界规范**。
> 招新答辩时你就按下面的顺序一个个介绍自己的类，评委一听就知道你懂"企业级分层"，加分爆炸 ✨。

### 3.1 顶层框架支撑：启动类 + 配置类（2 个）

#### ① 启动类：`UserManagementApplication.java`

参考文件：[UserManagementApplication.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/UserManagementApplication.java)

- `@SpringBootApplication` = 一个注解等于 3 个（面试背！）：
  - `@Configuration`：本类是配置类，可以 `@Bean` 注册对象到 IOC 容器
  - `@EnableAutoConfiguration`：✨ 最核心魔法！Spring Boot "开箱即用"：classpath 有 H2 驱动就自动建 DataSource；有 spring-web 就启动内嵌 Tomcat；有 Lombok 就识别
  - `@ComponentScan`：从本包 `com.zeqi.usermanagement` 递归扫描所有 `@Controller/@Service/@Repository/@Component`，自动实例化=Bean
- 启动方式 3 选 1：
  - IDEA：右键类 → Run（最常用，断点调试方便）
  - 命令行：`mvn spring-boot:run`（不用手动配 Maven 环境，IDEA 自带 wrapper 也行）
  - 打 jar：`mvn clean package` → `java -jar target/user-management-1.0.0-SNAPSHOT.jar`（生产部署）

#### ② 认证三件套 + 配置：`@AuthRequired` / `AuthInterceptor` / `WebMvcConfig`

参考文件：
- [AuthRequired.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/annotation/AuthRequired.java)
- [AuthInterceptor.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/interceptor/AuthInterceptor.java)
- [WebMvcConfig.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/config/WebMvcConfig.java)

**Spring AOP 思想实现登录态 = Flask 装饰器的 Java 版升级：**

```
 (1) 定义 @AuthRequired 注解    →   给"需要登录的方法/类"打标签
 (2) 写 AuthInterceptor         →   每次请求前反射：当前方法有这个标签吗？
                                    · 有 → 读 Header sessionId → 查 SessionManager
                                         · 查到 = ThreadLocal<Long> 塞 userId，放行
                                         · 查不到 = response.getWriter().write(Result.fail(401).json)，拦截
                                    · 无 → 直接放行(注册/登录)
 (3) WebMvcConfig 注册拦截器     →   addPathPatterns("/**") 先全拦
                                    excludePathPatterns("/api/auth/**", "/h2-console/**") 再放行公开接口
                                    + 顺带加 CORS 跨域（前后端分离开发必备）
```

⚠️ **ThreadLocal 超级大坑（面试必考！）**：`AuthInterceptor.afterCompletion()` **必须调 `SessionManager.clearCurrentUserId()` 清 ThreadLocal**。
因为 Tomcat 用线程池：一个 HTTP 请求结束后，线程不销毁，归还线程池，下一次另一个用户的请求会复用这个线程 → 如果不清，`ThreadLocal.get()` 拿到**上一个用户的 userId** = 严重越权漏洞（P0 事故）。

#### ③ 全局异常：`GlobalExceptionHandler.java`

参考文件：[GlobalExceptionHandler.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/config/GlobalExceptionHandler.java)

```
@RestControllerAdvice = @ControllerAdvice + @ResponseBody
作用：所有 Controller/Service 抛的异常，按类型匹配下面的 @ExceptionHandler 方法：
  · BusinessException       → Result.fail(e.code, e.message)        （业务自定义）
  · MethodArgumentNotValid  → Result.fail(400, "用户名不能为空")    （@NotBlank 校验失败）
  · HttpMessageNotReadable  → Result.fail(400, "JSON 语法错误")     （Body 没写 / Content-Type 没选 JSON）
  · Exception（兜底）       → Result.fail(500, "服务器内部错误")    （NPE/IOE 等），堆栈只打 log 不返回前端
```

好处 = 代码极度干净：Service 里只写 `throw BusinessException.conflict("用户名重复")`，不用每个方法都 `if(err) return Result.fail(...)` 写 100 次。

---

### 3.2 数据对象定义：Entity + DTO（5 个 Java 文件）

#### ① Entity 实体：`User.java` ↔ DB `users` 表
参考文件：[User.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/entity/User.java)
- `@Data`（Lombok 注解）= 一键生成 5 个样板代码：无参构造 + Getter/Setter 全部字段 + toString + equals/hashCode
- **为什么用 `LocalDateTime` 不用 `Date`？**（面试题）JDK8+ 新时间 API `java.time` 不可变/线程安全；`Date` 可变且方法大多 `@Deprecated`，还要配 `SimpleDateFormat` 线程不安全。

#### ② DTO：`Result<T>` 统一响应外壳
参考文件：[Result.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/dto/Result.java)
```java
// 3 字段 = Python 版每次都手动 new 的 dict：{code,message,data}
Integer code;   // HTTP 语义状态码（200/201/400/401/404/409）
String message; // 中文提示
T data;         // 泛型：塞 UserVO / LoginVO / List<UserVO> / Map / null 都行
// + 4 个 static 工厂：Result.success(...) / Result.fail(...)
```
统一响应 = 前后端契约化：前端 axios 拦截器里 `if(res.code!==200 && res.code!==201) showErrorToast(res.message)`。

#### ③ DTO：`RegisterDTO` / `LoginDTO` = 请求体入参
参考文件：
- [RegisterDTO.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/dto/RegisterDTO.java)
- [LoginDTO.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/dto/LoginDTO.java)
```java
@NotBlank(message = "用户名不能为空")
@Size(min=1, max=50, message="用户名长度 1-50 字符")
private String username;
```
- Controller 方法上贴 `@Valid` → 触发校验 → 失败直接抛异常 → 全局异常 400 中文提示 ✅

#### ④ 🔒 关键安全 DTO：`UserVO`（View Object）= 给前端看的白名单
参考文件：[UserVO.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/dto/UserVO.java)
```java
// 字段只有：id / username / createdAt / updatedAt
// ❌ 绝对没有 password！
public UserVO(User user) { /* Entity → VO 的脱敏转换构造器 */ }
```
- ⭐ **招新考试加分点**：**绝对不能直接 return User Entity 给前端**（Jackson 会把 password 也序列化出去）
  业界标准做法：**Entity 只在 Service/DAO 层流通；返回前端必须手动 new VO 白名单**。

#### ⑤ `LoginVO` / `AccountDTOs` 静态内部类
参考文件：
- [LoginVO.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/dto/LoginVO.java) = UserVO 基础上多一个 `sessionId` 字段
- [AccountDTOs.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/dto/AccountDTOs.java) = 3 个请求体合并成 1 文件（改用户名 `UpdateUsernameDTO` / 改密码 `UpdatePasswordDTO(oldPwd+newPwd)` / 注销 `DeleteAccountDTO(确认密码二次校验)`）

---

### 3.3 Repository 层：手写 SQL 最直观（`UserRepository.java` · JdbcTemplate）
参考文件：[UserRepository.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/repository/UserRepository.java)

```
为什么选 JdbcTemplate 不选 MyBatis / Spring Data JPA？
  1️⃣ 招新题考 SQL 基本功：JdbcTemplate 就是 100% 你手写 SQL，评委一眼能看到你 SQL 用了 PreparedStatement 预编译防注入
  2️⃣ 零门槛：和 Python sqlite3.execute(SQL, params) 一模一样；MyBatis 要学 XML/Provider/JPA 要学 JPQL，时间成本高

关键 API 对照表（JdbcTemplate ↔ Python sqlite3）：
  · jdbcTemplate.query(sql, BeanPropertyRowMapper<>(User.class), id)
     = cursor.execute(sql, (id,)) → rows → 自动列名(下划线 created_at → 驼峰 createdAt)映射到 User 字段
  · jdbcTemplate.update(sql, p1, p2, p3)
     = cursor.execute → return int 影响行数
  · KeyHolder + Statement.RETURN_GENERATED_KEYS
     = cursor.lastrowid (拿自增主键 id)
```

方法速览（CRUD 全覆盖）：
- 查：`findById / findByUsername / findAllOrderById / countByUsernameAndIdNot(改用户名查重时排除自己)`
- 写：`insert(返回主键) / updateUsername / updatePassword / deleteById`

---

### 3.4 Service 层：所有业务在这里（`UserService.java` + `SessionManager.java`）
参考文件：
- [UserService.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/service/UserService.java)
- [SessionManager.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/service/SessionManager.java)

#### SessionManager：内存 Session 存储
```java
@Component  // Spring 单例 Bean
public class SessionManager {
    // ConcurrentHashMap = 线程安全 HashMap，Tomcat 多线程并发读写不挂
    private final Map<String, Long> SESSIONS = new ConcurrentHashMap<>();
    // ThreadLocal<Long> = 每个线程(=每个请求)独立一份变量，存"当前请求的登录 userId"
    private static final ThreadLocal<Long> CURRENT_USER_HOLDER = new ThreadLocal<>();
}
```

#### UserService 7 个业务方法（⭐ 答辩重点：一个一个讲）：

| 方法 | @Transactional | 业务逻辑步骤 | 抛什么异常？ |
|---|---|---|---|
| `register(RegisterDTO)` | ✅ | ① findByUsername 查重 →② 组装 User →③ JdbcTemplate INSERT →④ 查回转 UserVO | 409 Conflict（重名） |
| `login(LoginDTO)` | ❌（只读）| ① findByUsername →② password equals 比对 →③ UUID.random() sessionId →④ SessionManager.save →⑤ LoginVO | 401 Unauthorized（账号密码错，不区分哪错=防爆破枚举） |
| `getCurrentUser(userId)` | ❌ | findById → UserVO | 404（用户可能已注销）|
| `listAllUsers()` | ❌ | findAll → Stream().map(UserVO::new).collect(Collectors.toList())（Python 列表推导式 Java 版）| - |
| `updateUsername(userId, dto)` | ✅ | ① countByUsernameAndIdNot 排除自己查重 →② updateUsername SQL →③ 查回最新 | 409（别人已占用） |
| `updatePassword(userId, dto)` | ✅ | ① 新旧相同? 400 →② findById + 原密码校验 →③ updatePassword →④ **SessionManager.invalidateAllByUserId(踢所有设备下线)** ✨ | 400 / 401 |
| `deleteAccount(userId, dto)` | ✅ | ① 二次确认密码 →② deleteById →③ SessionManager.invalidateAllByUserId | 401 |

> 🔺 **改密码/注销踢下线 = 招新题加分项**：很多同学只清了"当前 sessionId"，我们清"该 userId 对应的所有 sessionId"，用 Stream + filter + removeAll。
> 生产环境这个逻辑要同步到 Redis：`SCAN 0 MATCH session:*` + `DEL key` 思路一样。

---

### 3.5 Controller 层：7 个 API 路由（2 个文件）
参考文件：
- [AuthController.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/controller/AuthController.java)
- [UserController.java](file:///e:/my_file/zeqi2026/user-management/src/main/java/com/zeqi/usermanagement/controller/UserController.java)

```java
// 【AuthController：公开接口 = 不加 @AuthRequired】
@RestController   // @Controller + @ResponseBody：所有方法直接序列化为 JSON
@RequestMapping("/api/auth")
public class AuthController {
    @PostMapping("/register")
    public ResponseEntity<Result<UserVO>> register(@Valid @RequestBody RegisterDTO dto) {
        UserVO user = userService.register(dto);
        Result<UserVO> body = Result.success(201, "注册成功", user);
        return ResponseEntity.status(HttpStatus.CREATED).body(body);
        // ⚠️ 为什么用 ResponseEntity？
        // Result.code=201 只是 Body 里的字段；题目要求"HTTP 响应头的 Status Code"也要是 201
        // ResponseEntity 可以同时设置"响应头状态码 + 响应体 JSON"= RESTful 语义正确 ✨
    }

    @PostMapping("/login")
    public Result<LoginVO> login(@Valid @RequestBody LoginDTO dto) {
        return Result.success("登录成功", userService.login(dto));
    }
}

// 【UserController：需要登录 = 类级别贴 @AuthRequired，5 个方法默认全校验】
@RestController
@RequestMapping("/api/users")
@AuthRequired   // 🔐 一贴顶 5 个
public class UserController {
    private Long me() { return SessionManager.getCurrentUserId(); } // 从 ThreadLocal 拿当前用户

    @GetMapping("/me")              public Result<UserVO> me() { ... }
    @GetMapping                    public Result<Map<String,Object>> list() { ... users + total }
    @PatchMapping("/username")      public Result<UserVO> updateUsername(@Valid @RequestBody ...) { ... }
    @PatchMapping("/password")      public Result<Void> updatePassword(...) { /* 踢下线后提示重新登录 */ }
    @DeleteMapping("/me")           public Result<Void> deleteAccount(...) { ... }
}
```

---

---

## 4. 数据库 & 配置文件详解（pom.xml / application.yml / schema.sql）

### 4.1 核心配置：`pom.xml`（Maven 工程灵魂）
参考文件：[pom.xml](file:///e:/my_file/zeqi2026/user-management/pom.xml)

**依赖选型表（= Python requirements.txt 的 Java 对应版）**：

| 依赖 | 作用 | 对应的 Python 库 |
|---|---|---|
| `spring-boot-starter-web` | Spring MVC + 内嵌 Tomcat + Jackson JSON | `Flask` + `Werkzeug` |
| `spring-boot-starter-jdbc` | JdbcTemplate + DataSource 自动配置 + HikariCP 连接池 | `sqlite3` 标准库 + 连接池 |
| `com.h2database:h2 (scope=runtime)` | H2 嵌入式数据库（纯 Java 零安装，语法 99% MySQL 兼容） | `sqlite3` 驱动本体 |
| `org.projectlombok:lombok (optional)` | 自动 Getter/Setter/日志，样板代码 -80% | 无（Python 动态类型本就无样板） |
| `spring-boot-starter-validation` | `@NotBlank/@Size` JSR-303 参数校验 | `marshmallow` / `pydantic` |

构建插件 `spring-boot-maven-plugin`：
- 提供 2 个神仙命令：
  - `mvn spring-boot:run` = 一键启动（相当于 `python app.py`）
  - `mvn clean package` = 打 `target/user-management-1.0.0-SNAPSHOT.jar`
  - **这个 jar 内嵌 Tomcat**：拷到任何有 JDK 8+ 的机器 → `java -jar xxx.jar` 直接跑，不用装 Tomcat！这就是 Spring Boot "可执行 jar" 最香的地方 ✨

### 4.2 Spring Boot 配置：`application.yml`
参考文件：[application.yml](file:///e:/my_file/zeqi2026/user-management/src/main/resources/application.yml)

**重要配置速查（面试能讲出每个 key 的意义 = 加分）**：

| YAML key | 值 | 解释 |
|---|---|---|
| `server.port` | 5000 | 端口，和 Python 版一致，Postman 不用改 |
| `spring.datasource.url` | `jdbc:h2:file:./data/usersdb;MODE=MySQL` | H2 文件模式持久化；`MODE=MySQL` = 语法 99% 和 MySQL 一样（方便你以后切 MySQL 只需改这一行 + 换 mysql-connector-java 依赖） |
| `spring.sql.init.mode=always` | always | 每次启动都跑 schema.sql + data.sql（配合 `IF NOT EXISTS` 安全） |
| `spring.h2.console.enabled=true` | true + path=`/h2-console` | 💡 招新答辩神器！打开浏览器：http://127.0.0.1:5000/h2-console 就能像 Navicat 一样查 users 表，演示"注册成功后 DB 真的多了一行"= 评委眼前一亮 |
| `logging.level.JdbcTemplate=DEBUG` | DEBUG | 控制台打印 **每条执行的 SQL + 参数**，调试时你能一眼看到 JdbcTemplate 发了什么 SQL，和招新题 SQL 教程联动学 |

### 4.3 建表 & 初始化：`schema.sql` + `data.sql`
参考文件：
- [schema.sql](file:///e:/my_file/zeqi2026/user-management/src/main/resources/schema.sql)
- [data.sql](file:///e:/my_file/zeqi2026/user-management/src/main/resources/data.sql)

```sql
-- schema.sql（启动自动执行）
CREATE TABLE IF NOT EXISTS users (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    username    VARCHAR(50)  NOT NULL UNIQUE,   -- UNIQUE 约束 = DB 层面兜底防重名（并发时 Service 先查再插有竞态，再加 UNIQUE 双保险）
    password    VARCHAR(255) NOT NULL,          -- 题目允许明文；生产必须 BCrypt：$2a$10$N9qo...
    created_at  TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- data.sql：MERGE INTO = H2 的"插入或忽略"（SQLite=INSERT OR IGNORE；MySQL=INSERT IGNORE），启动多次 admin 只插一次
MERGE INTO users (id, username, password) KEY(id) VALUES (1, 'admin', 'admin123');
```

🔑 **UNIQUE 双重保险面试题**：Service 层明明 `findByUsername` 先查再插了，为什么 DB 还要加 `UNIQUE` 约束？
→ 因为**并发竞态条件**：两个请求 T1/T2 同时注册"zhangsan"，都查 findByUsername=空 → 两个都认为可以注册 → 都 INSERT → 没 UNIQUE=DB 里两行"zhangsan"= 数据脏了；加 UNIQUE = DB 在引擎层兜底，第二个 INSERT 一定会抛唯一键冲突 → 全局异常捕获转 409。**先查后插只能 99% 防重，100% 防重必须靠 DB UNIQUE 约束**。

---

---

## 5. 🔥 Postman 接口测试 · 手把手图文教程（重点！共 7 个接口+异常场景）

> 🎯 **这一节是本项目的灵魂**：招新题最后一条明确要求「请使用 Postman 测试接口，并附上测试结果截图」。
> 👉 **因为 Java 版和 Python 版 API 完全一致**（端口 5000、URL、Body 字段、响应 JSON 一个字没改），所以下面 Postman 操作步骤 100% 复用！
> 唯一变化：前置启动命令从 `python app.py` 改成了「先装 JDK8+/Maven → `mvn spring-boot:run`」

### 5.0 前置准备（一次性做）

#### ① 安装 Postman
- 官网下载：https://www.postman.com/downloads/ → 选 Windows 64 位安装版
- 第一次打开会让你登录/注册账号：**可以直接点「Skip and go to the app」跳过**（免费版功能完全够用）

#### ② 先把 Java Spring Boot 项目跑起来（3 种方式任选其一）

> ⚠️ 前置条件：本机先装 **JDK 8 或 11/17**（推荐 JDK8，招新考试标配）和 **Maven 3.6+**（或者用 IDEA 自带的 Maven Wrapper 不用额外装）
> 环境装好后，`java -version` 应该能看到 1.8.x；`mvn -v` 能看到 Apache Maven x.x.x

##### 方式 A：命令行启动（最通用，Windows PowerShell 直接跑）
```powershell
cd e:\my_file\zeqi2026\user-management       # ① 进 user-management 目录（pom.xml 所在目录！）
mvn spring-boot:run                            # ② 一键启动！第一次会下载所有 jar 包（1~3 分钟，以后秒启动）
```

##### 方式 B：IDEA 启动（⭐ 推荐，最方便调试 + 看 SQL 日志）
1. IDEA File → Open → 选 `e:\my_file\zeqi2026\user-management\pom.xml` → Open as Project
2. 右下角 Maven 会自动 Reload（弹框 Enable Auto-Import 选是）→ 依赖自动下载
3. 左侧找到 `UserManagementApplication.java` → **右键类名 → Run 'UserManagementApplication'**
4. 下方 Console 栏看日志：出现 **"Started UserManagementApplication in X.XXX seconds"** + 没有 ERROR = 启动成功 ✅

##### 方式 C：打 jar 包后 java -jar 运行（模拟生产部署）
```powershell
cd e:\my_file\zeqi2026\user-management
mvn clean package -DskipTests      # 打包跳过单元测试（我们现在没写 JUnit）
java -jar target/user-management-1.0.0-SNAPSHOT.jar
```

**启动成功标志（看到这些就 OK，窗口别关！）**：
```
2026-09-04 10:00:00.000  INFO 12345 --- [  restartedMain] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat started on port(s): 5000 (http) with context path '/'
2026-09-04 10:00:00.010  INFO 12345 --- [  restartedMain] c.z.u.UserManagementApplication           : Started UserManagementApplication in 2.876 seconds (JVM running for 3.1415)
```
顺便可以先打开 H2 控制台看看初始 admin 账号是否存在（演示给评委看的神器）：
```
浏览器访问：http://127.0.0.1:5000/h2-console
  JDBC URL: 填 jdbc:h2:file:./data/usersdb;AUTO_SERVER=TRUE;MODE=MySQL
  User Name: sa
  Password: （空）
  → 点 Connect → 左侧能看到 USERS 表 → SELECT * FROM USERS; 就能看到 admin 账号了 ✨
```

#### ③ 在 Postman 里建一个「Collection 文件夹」方便整理
1. 左侧 Collections → 点 **+** 号新建 Collection
2. 起名：`用户管理系统-择栖招新2026`，Description 写 `Spring Boot 2.7 + JDK8 + H2`
3. **（神操作）设置 Collection 变量，后面不用反复复制粘贴 URL 前缀和 sessionId：**
   - 点 Collection 名字 → 切到 **Variables** Tab → 加 2 个变量：
   | Variable | Initial value | Current value |
   |---|---|---|
   | `baseUrl` | `http://127.0.0.1:5000` | 同左 |
   | `sessionId` | （空，登录后再填） | （空） |
   - 保存（Ctrl+S）后，后续 URL 写 `{{baseUrl}}/api/auth/register` 即可，一劳永逸！

---

### 5.1 ✅ 接口 1：注册（POST /api/auth/register）→ 截图【1】

#### 操作步骤：
1. 左侧 Collection 点 **Add request** → 起名：`1. 注册（成功）`
2. Method 下拉框选 **POST**，URL 填：`{{baseUrl}}/api/auth/register`
3. 切到 **Body** Tab → 选 **raw** → 最右侧下拉选 **JSON**（⚠️ 这步新手最容易错！默认是 Text，Spring Jackson 解析不到 → 报 400"请求体格式错误"）
4. 粘贴 JSON：
   ```json
   {
     "username": "zhangsan",
     "password": "123456"
   }
   ```
5. 右上角蓝色 **Send** 按钮 🔵

#### 你要看到的结果（= 截图【1】内容 · 红框标这 3 处）：
```
┌─ Status: 201 Created ──────────────────────────────────────────────────────┐ ← 🟥 红框①：201！不是200！
│ Time: 32ms   Size: 280B                                                     │
├─ Body（Pretty/JSON 视图 Tab）───────────────────────────────────────────────┤ ← 🟥 红框②：Body JSON
│ {                                                                           │
│   "code": 201,                                                              │
│   "message": "注册成功",                                                    │
│   "data": {                                                                 │ ← 🟥 红框③：data 里 ✅没有 password 字段！
│     "id": 2,                                                                 │
│     "username": "zhangsan",                                                 │
│     "createdAt": "2026-09-04T10:00:00",                                     │
│     "updatedAt": "2026-09-04T10:00:00"                                      │
│   }                                                                         │
│ }                                                                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 🔁 再测异常场景（截图【1-b】：409 重名，招新题明确要求的边界情况）
- 不改任何东西，直接再点一次 Send（同一个 zhangsan 再注册一次）
- **期望**：Status **409 Conflict**，Body 里 message = `用户名 'zhangsan' 已存在，请换一个` ✅
- （进阶加分：这时候打开 H2 Console 刷新，只能看到 1 条 zhangsan，证明 UNIQUE 约束 + 事务生效了 ✨）

---

### 5.2 ✅ 接口 2：登录（POST /api/auth/login）→ 截图【2】

#### 操作步骤：
1. 新建 Request 起名：`2. 登录（成功）`
2. **POST** `{{baseUrl}}/api/auth/login`
3. Body → raw → JSON：
   ```json
   {"username": "admin", "password": "admin123"}
   ```
   （用 data.sql 初始化的 admin 测试账号；也可以用上一步刚注册的 zhangsan / 123456）
4. Send

#### 你要看到的结果（= 截图【2】· 这串 sessionId 是后续 5 个接口的钥匙 🗝️）：
```
┌─ Status: 200 OK ────────────────────────────────────────────────────────────┐
├─ Body（JSON）───────────────────────────────────────────────────────────────┤
│ {                                                                           │
│   "code": 200,                                                              │
│   "message": "登录成功",                                                    │
│   "data": {                                                                 │
│     "id": 1,                                                                │
│     "username": "admin",                                                    │
│     "createdAt": "...", "updatedAt": "...",                                 │
│     "sessionId": "a1b2c3d4-5678-90ef-ghij-klmnopqrstuv"  ← 🟥 红框：复制这串！
│   }                                                                         │
│ }                                                                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### ⚡ 关键一步：存 sessionId 到 Collection 变量
- 复制响应体里的 `sessionId`（不带引号）
- 点 Collection → Variables → 粘贴到 `sessionId` 变量的 **Current Value** 列 → Save
- （进阶神操作：Postman 支持在 Tests Tab 写一行 JS 自动存变量：
  `pm.collectionVariables.set("sessionId", pm.response.json().data.sessionId);`
  → 点 Send 后 sessionId 自动填到变量里，连复制粘贴都省了！招新答辩演示这个超加分 ✨）

#### 🔁 测异常（截图【2-b】：401 账号错）
- 把请求体 password 改成 `wrongpassword` → Send
- 期望：**401 Unauthorized**，message=`用户名或密码错误`（⚠️ 故意不告诉你是"用户名不存在"还是"密码错"——安全最佳实践：防爆破枚举用户名）

---

### 5.3 ✅ 接口 3：查看自己（GET /api/users/me）→ 截图【3】· 重点！必须传 Header

> 📌 **招新题硬性考点**：「查看当前登录用户的信息（调用接口时在请求头携带 sessionId）」

#### 操作步骤（最重要的一步：加 Header！）：
1. 新建 Request：`3. 查看当前用户（带sessionId，成功）`
2. **GET** `{{baseUrl}}/api/users/me`
3. **⚠️ 切到 Headers Tab** → Key 列填：`sessionId`，Value 列填：`{{sessionId}}`（引用变量，或直接粘贴上一步复制的）
4. **Body 不用填（GET 正常不带 Body）**，Send

#### 成功响应（截图【3】）：
- Status: **200 OK**
- Body: data = admin 用户对象（id=1）

#### 🔁 两个**必测异常场景**（题目明确要求：查不到/为空 sessionId → 返回 401）：

| 场景 | 操作 | 预期 = 截图【3-b】&【3-c】 |
|---|---|---|
| **不传 sessionId** | Headers 里把 sessionId 那一行**取消勾选**（不发送）→ Send | Status **401** Unauthorized + message=`"未登录或 sessionId 无效，请重新登录"` ✅（题目明确要求！） |
| **传错 sessionId** | sessionId Value 填 `fake-wrong-id-123` → Send | Status **401**（同上）✅ |

> 🔍 看 Spring 控制台 DEBUG 日志能看到：不传 sessionId 时 `AuthInterceptor` 直接写了 401，`DispatcherServlet` 根本没进 `UserController.me()` = 拦截器真的生效了（答辩时可以指给评委看）。

---

### 5.4 ✅ 接口 4~7（加分题）：用户列表 + 改用户名 + 改密码 + 注销 → 截图【4】~【7】

> 操作套路完全一样，只改 Method / URL / Body，列表如下（**⭐ 全部都要带 sessionId Header！**因为 UserController 类级别贴了 `@AuthRequired`）：

| 截图编号 | 请求名（建议命名） | Method | URL | Body JSON | 成功 Status & 结果 |
|---|---|---|---|---|---|
| 【4】 | 4. 用户列表（加分） | **GET** | `{{baseUrl}}/api/users` | 无 | 200 → `{"total": 2, "users": [ {admin对象}, {zhangsan对象} ]}` |
| 【5】 | 5. 修改用户名（加分） | **PATCH** | `{{baseUrl}}/api/users/username` | `{"username":"zhangsan_new"}`（先登 zhangsan 的号测这个） | 200 → username 变成新值；再截一张改成 admin 的= **409 冲突**的图 |
| 【6】 | 6. 修改密码（加分） | **PATCH** | `{{baseUrl}}/api/users/password` | `{"oldPassword":"admin123","newPassword":"newpass456"}` | 200 → message=`"密码修改成功，请使用新密码重新登录"`；**再调任何接口 → 401！（踢所有设备下线，加分点⭐）** |
| 【7】 | 7. 注销账号（加分） | **DELETE** | `{{baseUrl}}/api/users/me` | `{"password":"admin123"}`（二次确认密码） | 200 → message=`"账号已注销，感谢使用"`；再用 H2 Console 查：这行真的 DELETE 掉了 ✅；再调 /me = 401 |

> 💡 **截图加分小技巧（评委一看就觉得很专业）**：
> - 每次 Send 完，Postman 右上角会自动显示请求时间/大小/状态码，这些都要截图进去（证明真的调过，不是伪造的 JSON）
> - **改密码 / 注销**后，记得**再截一张「再次调用 /users/me → 401」**的截图，证明 session 真的被清了（改密码=踢所有设备下线，这个细节很多同学忘记做 = 丢加分 ⭐）
> - **改用户名 409 冲突**、**改密码原密码错误 401**、**注销确认密码错 401** 这些**边界错误截图**各多截一张 = 对应招新题要求的"处理边界情况与状态码规范" ✅

---

### 5.5 🧪 完整测试用例 Checklist（交作业前打勾，保证题目第 6 步「边界情况」全覆盖）

```
✅ 【注册】
  [正常] 新用户名 zhangsan → 201 + DB 新增（H2 Console 能查到）
  [异常] 同名再注册一次  → 409 Conflict            ← 题目明确要求
  [异常] username 为空字符串 " " / 不传 username → 400 参数校验失败
  [异常] Body 没写 / Content-Type 选 Text 没选 JSON → 400 请求体格式错误

✅ 【登录】
  [正常] admin / admin123  → 200 + 拿到 sessionId
  [异常] 密码错 / 用户名不存在 → 统一 401 Unauthorized （防爆破枚举）
  [异常] 空 password → 400

✅ 【查看自己】⭐ 招新题硬性要求重点！
  [正常] 带正确 sessionId Header → 200 + 自己信息（password 不存在）
  [异常] 不带 sessionId        → 401             ← 题目明确要求
  [异常] 带伪造 sessionId      → 401             ← 题目明确要求

✅ 【用户列表】(选做加分)
  [正常] 带 sessionId → 200 + total / users 数组

✅ 【改用户名】(选做加分)
  [异常] 改成已存在的 "admin" → 409 Conflict
  [正常] 改成没人用的新名 → 200 + 更新后对象（H2 再查，updatedAt 刷新=加分细节）

✅ 【改密码】(选做加分)
  [异常] 原密码错 → 401
  [异常] 新密码=原密码 → 400
  [正常] 修改成功 → 200 → 再调 /me = 401（踢所有设备下线⭐加分点）

✅ 【注销】(选做加分)
  [异常] 确认密码错 → 401
  [正常] 确认密码对 → 200 注销成功 → H2 Console SELECT 查不到该用户 + /me=401
```

---

---

## 6. 最终运行 & 提交检查清单

### 6.1 本地一键启动命令备忘录（Java Spring Boot 版）

```powershell
# ========== 环境确认（第一次执行 1 次） ==========
java -version     # JDK 必须 1.8 以上（输出 java version "1.8.0_xxx" 就 OK）
mvn -v            # Maven 3.6+（输出 Apache Maven 3.9.x 就 OK；IDEA 自带则无需手动装）

# ========== 每次开始开发 / 调接口 ==========
cd e:\my_file\zeqi2026\user-management
mvn spring-boot:run
# 启动成功标志 = 控制台看到：
#   "Tomcat started on port(s): 5000 (http)"
#   "Started UserManagementApplication in X.XXX seconds"
# 别关窗口！关了 = Tomcat 停了 = Postman 报 ECONNREFUSED

# ========== 打包发布（部署服务器用） ==========
cd e:\my_file\zeqi2026\user-management
mvn clean package -DskipTests
java -jar target/user-management-1.0.0-SNAPSHOT.jar
```

### 6.2 项目提交清单（对应招新题 Part2 的「提交内容」要求）

| # | 要提交的东西 | 本项目对应位置 | 检查打勾 |
|---|---|---|---|
| 1 | GitHub 仓库 URL（写 .txt） | `https://github.com/yaya7777777/zeqi2026.git`（之前已连好） | ✅ 直接 git push |
| 2 | **代码文件**（用户管理系统） | `user-management/` 整个 Maven 目录（pom.xml + src/ 是核心；target/ / data/ 可加 .gitignore 不提交） | ✅ |
| 3 | **本说明文档**（本文档 4.md） | `4.md`（含 2 道简答题 + Java Spring Boot 架构讲解 + Postman 步骤） | ✅ |
| 4 | **Postman 接口测试截图**（题 3 明确要求） | 建议新建文件夹 `user-management/screenshots/`，按「5.1~5.7」顺序命名：`1-1注册成功.png / 1-b注册409重名.png / 2登录成功.png / 2-b登录401失败.png / 3查看自己成功.png / 3-b不传sessionId401.png / 3-c传错sessionId401.png / 4用户列表.png / 5改用户名.png / 6改密码.png / 6-b改密码后调接口401.png / 7注销.png` | ⚠️ **你自己用 Win+Shift+S 截一下！**我没法访问你桌面截图 |
| 5 | （可选加分）Postman Collection JSON 导出 | Postman 点 Collection → 右上角 `…` → Export → Collection v2.1 → 导出为 `user-management/用户管理系统-择栖招新2026.postman_collection.json`（评委双击就能导入，连 URL 都不用自己填=超加分 ⭐） | ➕ 强烈建议导 |
| 6 | （可选加分）H2 Console 演示图 | http://127.0.0.1:5000/h2-console 注册完 zhangsan 后截一张 `SELECT * FROM USERS;` 的结果页面（证明真的落库了，不是光 Postman 显示成功） | ➕ 演示给评委看效果爆炸 ✨ |

### 6.3 `.gitignore` 建议（加在 `user-management/.gitignore`，这几个目录不要 push 到 GitHub）
```gitignore
# Maven 编译产物
target/
# H2 数据库文件（自己本地的测试数据）
data/
*.mv.db
*.trace.db
# IDEA / Eclipse 工程文件（每个人自己的 IDE 自动生成）
.idea/
*.iml
.classpath
.project
.settings/
# 操作系统垃圾
Thumbs.db
.DS_Store
# 日志
*.log
```

### 6.4 Git 一键推送（之前连好 origin，只需 3 行）

```powershell
cd e:\my_file\zeqi2026
git add -A
git commit -m "feat: 完成用户管理系统(Spring Boot 7 API + 4.md 教程+Postman测试文档+Java教学注释)"
git push origin main
```

---

> 🎉 **恭喜！** 看到这里，你已经：
> - ✔️ 背会了「用户注册 10 步行程(Spring 版) + 持久化 7 个维度」两道高频面试简答题
> - ✔️ 从 0 到 1 手写了一个**工业级 Maven 分层 Spring Boot 用户管理系统**（10 个 Java 类职责清晰 + 7 个 API + 全局异常 + 登录拦截器 + 边界异常全处理）
> - ✔️ 理解每个核心类在 3 层架构里的作用 + 为什么这么分层（招新答辩时按 3.1~3.5 顺序介绍，评委一听就懂 ✨）
> - ✔️ 知道每个接口在 Postman 里怎么填，截图截哪一块（正常+异常都截=题目「边界情况处理」满分）
>
> 最后就差你 3 件事：
> 1. ✅ 本机装 JDK8 + Maven → 跑 `mvn spring-boot:run` 把项目真的启动起来
> 2. ✅ 打开 Postman 按 5.1~5.7 实际点一遍 Send + **Win+Shift+S 截图保存到 screenshots 文件夹**
> 3. ✅ （可选加分）打开 `http://127.0.0.1:5000/h2-console` 截一张 users 表有数据的 SQL 查询图
>
> 加油！你已经完成 99% 啦 🚀
