# Part 2 · 项目实战：用户管理系统（含完整 Postman 接口测试教程）

> 📚 配套代码目录：`../user-management/`
> 📚 关联文档：前面完成的 `2.md`（HTTP 基础 + RESTful API 设计）、`3.md`（数据库 + SQL 教程）
> 🧪 配套测试工具：**Postman**（官网下载免费版 https://www.postman.com/downloads/ ）

---

## 目录
1. [简答题（2 道，详细讲解）](#1-简答题2-道详细讲解)
2. [项目架构总览（3 层架构图画给你看）](#2-项目架构总览3-层架构图画给你看)
3. [代码逐段讲解：`app.py` 从 0 到 1](#3-代码逐段讲解apppy-从-0-到-1)
4. [数据库表设计：`init_db.sql` 详解](#4-数据库表设计init_dbsql-详解)
5. [🔥 Postman 接口测试 · 手把手图文教程（重点！共 7 个接口+异常场景）](#-postman-接口测试--手把手图文教程重点共-7-个接口异常场景)
6. [最终运行 & 提交检查清单](#6-最终运行--提交检查清单)

---

---

## 1. 简答题（2 道，详细讲解）

### ① 请描述一次"用户注册"的完整流程：从客户端发起 HTTP 请求，到数据写入数据库并返回响应，中间经历了哪些环节？

> 💡 **学习提示**：这是后端面试**超高频 100% 考题**，面试官通过这道题看你对"Web 请求全链路"有没有宏观认识。把下面 8 步**背熟**，再加一个自己画的时序图，面试稳过。

#### 「用户注册」8 步完整流程（时间从上到下 = 请求往返顺序）

```
  📱 客户端(浏览器/Postman)          🌐 网络层             🚀 后端服务器(Flask)         💾 数据库(SQLite/MySQL)
        │                              │                        │                             │
        │ 1. 组装 HTTP 请求           │                        │                             │
        │   方法:POST /api/auth/reg   │                        │                             │
        │   Header:Content-Type:JSON  │                        │                             │
        │   Body:{username,password}  │                        │                             │
        │ ─────────────────────────────────────────────────────────────────────────────────► │
        │                              │ 2. TCP 三次握手         │                             │
        │                              │    + TLS(HTTPS) 握手   │                             │
        │                              │    + 路由器/负载均衡   │                             │
        │                              │ ──────────────────────►│                             │
        │                              │                        │ 3. 解析 HTTP 请求          │
        │                              │                        │    (Flask/Werkzeug 解析)  │
        │                              │                        │    → 拿到 request 对象    │
        │                              │                        │                             │
        │                              │                        │ 4. 中间件/过滤器          │
        │                              │                        │    (参数校验/限流/CORS)   │
        │                              │                        │                             │
        │                              │                        │ 5. 业务逻辑处理           │
        │                              │                        │    • 判空 username/pwd   │
        │                              │                        │    • UNIQUE 查是否重名    │
        │                              │                        │ ─────────────────────────►│
        │                              │                        │ 6. JDBC/SQLite3 驱动      │
        │                              │                        │    (SQL: INSERT users)    │
        │                              │                        │    (参数 ? 防注入)        │
        │                              │                        │                             │ 7. DB 执行 SQL
        │                              │                        │                             │    • 加行锁
        │                              │                        │                             │    • 写 Redo/Undo Log
        │                              │                        │                             │    • COMMIT 持久化到磁盘
        │                              │                        │ ◄─────────────────────────│
        │                              │                        │ 8. 封装 HTTP 响应          │
        │                              │                        │    Status: 201 Created    │
        │                              │                        │    Body: {code,msg,data} │
        │ ◄─────────────────────────────────────────────────────────────────────────────────│
        │ 9. 客户端拿到 201 → 跳到登录页 │                        │                             │
```

#### 面试标准答案（分 9 小点背）：

| 步骤 | 环节 | 要点 & 面试关键词 |
|---|---|---|
| **1** | **客户端组装请求** | HTTP 方法=`POST`(RESTful 创建语义)、URL=`/api/auth/register`、`Content-Type: application/json`、Body 序列化 JSON 含 `username/password` |
| **2** | **网络传输** | DNS 解析域名→IP；TCP 三次握手；HTTPS 额外 TLS1.3 握手(证书/RSA/ECDHE)；经过路由器、Nginx 负载均衡(多机部署时) |
| **3** | **Web 服务器解析 HTTP** | Flask 的 Werkzeug / Java 的 Tomcat 监听 80/5000 端口，把 TCP 字节流解析成 `Request` 对象(方法/URL/Header/Body) |
| **4** | **Filter/Interceptor 过滤器链** | CORS 跨域检查→限流(Rate Limit→429)→日志 TraceId→统一异常捕获(全局 ControllerAdvice) |
| **5** | **Controller 层接收** | `@PostMapping("/register")` 反序列化 Body 为 `RegisterDTO`，用 `@Valid` + `@NotBlank` 做参数校验→400 |
| **6** | **Service 层业务逻辑** | ① 空值校验 ② `SELECT id FROM users WHERE username=?` 查重→冲突抛 409 Conflict |
| **7** | **DAO/Mapper 写库** | `INSERT INTO users(username,password) VALUES(?,?)` 用**预编译语句 ? 占位符**（绝对不能字符串拼接→防 SQL 注入） |
| **8** | **DB 持久化** | MySQL 执行过程：解析器→优化器→执行器→InnoDB 引擎：Buffer Pool 写脏页→Redo Log(Write Ahead Log 崩溃安全)→Binlog(主从同步)→COMMIT |
| **9** | **返回响应** | DB 返回自增 ID→Service 查回 DTO(**过滤掉 password 字段！**安全红线)→Controller 返回 `ResponseEntity<>(201 Created)`→客户端渲染 |

> ⚠️ **3 个灵魂追问（面试官 90% 概率接着问）**：
> - Q：为什么用预编译 `?` 防 SQL 注入？A：SQL 结构和参数分离，即使参数里有 `' OR 1=1 --`，也会被当成"字符串值"而不是 SQL 语法执行。
> - Q：为什么返回 201 而不是 200？A：RESTful 语义：**201 CREATED 表示"服务器成功创建了新资源"**；统一约定比 200 更精准。
> - Q：为什么返回体里不能有 password？A：敏感字段严禁出现在响应 JSON 里（即使加密过也不该返回），Chrome DevTools / 抓包 / 中间人攻击都能看到。

---

### ② 为什么用户数据要持久化到数据库，而不是只存在程序内存中？

> 💡 **考点**：看你是否理解「**持久化 Persistence**」的本质意义（内存 vs 磁盘 8 大对比维度）。

#### 一句话核心答案：
> **内存是"易失性存储"，断电/重启即丢失；数据库是"持久化存储"，写入后即使断电重启数据永远存在。**
> 但面试只说这一句不够，要展开成下面 7 个维度（每个维度各举 1 个例子就满分）：

| 维度 | 内存（Java Heap / Python dict） | 数据库（MySQL / SQLite） | 举个实际反例 ❌（只用内存会怎样？） |
|---|---|---|---|
| **① 持久性** | ❌ 易失：进程退出 / 服务器断电 / `kill -9` 立即清空全部数据 | ✅ 持久：数据写在磁盘文件（SQLite=users.db；MySQL=ibd 文件），断电重启数据还在 | 凌晨运维重启了下机器 → 早上老板发现**所有注册用户全没了**，公司直接倒闭 |
| **② 容量** | ❌ 极小：单机一般 16G~64G，减去 JVM/其他程序，能存的数据有限（还要 GC 开销） | ✅ 无限：磁盘几 TB 随便加，分库分表后能存 PB 级（百亿行） | 你用 `HashMap` 存 1000 万用户 → JVM OOM `OutOfMemoryError` 直接崩溃，业务中断 |
| **③ 并发安全** | ❌ 手动加锁：多线程同时写同一条数据（比如扣余额），自己实现 `synchronized`/`ReentrantLock`，写错就超卖 | ✅ 原生 ACID 事务 + 行锁 / MVCC：`UPDATE account SET balance=balance-100 WHERE id=1 AND balance>=100` 并发安全 | 双 11 秒杀 1000 人同时抢同一件商品 → 内存里 HashMap 没锁 → **超卖 2000 份**，老板亏哭 |
| **④ 检索能力** | ❌ 全表扫：按 username 模糊搜索？你得遍历整个 HashMap 一个个 `equals()`，O(N) 复杂度，百万用户卡死 | ✅ B+ 树索引：`SELECT * FROM users WHERE username LIKE '张%'` 走索引，O(logN) 百万级毫秒返回 | 做一个用户搜索功能，输入"张"想找所有姓张的 → 内存写不出高效索引，页面卡 30 秒超时 |
| **⑤ 崩溃恢复** | ❌ 0 恢复：写到一半进程崩溃？刚才那 50% 成功 50% 失败的数据全乱套了，也没日志可恢复 | ✅ Write-Ahead Log：Redo Log + Undo Log + Binlog，崩溃后自动回滚未提交事务，保证数据一致性 | 用户支付订单：扣余额成功、创建订单成功，但**加积分前**崩溃重启 → 内存版变成用户钱没了订单也没了，投诉爆炸 |
| **⑥ 多进程共享** | ❌ 进程隔离：Python A 进程的 dict，Python B 进程 / Java 程序 / 管理后台 完全读不到（共享内存要自己写 IPC 巨麻烦） | ✅ 通用协议：任何语言（Go/Java/Python/Node）只要用 JDBC/SQLite3 驱动 + 标准 SQL 都能读写同一份数据 | 你用 Flask 起了 4 个 worker 进程（多核并发）→ 每个进程一份 Sessions dict → **用户在 A 机登录，下次被负载均衡打到 B 机就 401 登出** |
| **⑦ 生态工具** | ❌ 0 生态：自己写备份脚本？自己做主从同步？自己做导出 CSV？ | ✅ 生态完善：`mysqldump` 每日自动备份、主从复制读写分离、binlog 回滚闪回、DataGrip/Navicat 可视化管理 | 数据库文件误删了？→ SQLite 有 `.backup` 命令、MySQL 有 binlog2sql 闪回、云数据库有多可用区快照；内存版只能哭 |

> 🎯 **面试总结公式（套话）**：
> *"程序内存是计算资源，用于**临时运算**；数据库是存储资源，用于**永久保存状态**。二者配合使用：热数据放 Redis 内存缓存加速，冷数据落 MySQL 磁盘持久化，这就是经典的"内存 + 磁盘分层架构"。"*

---

---

## 2. 项目架构总览（3 层架构图画给你看）

### 2.1 项目目录结构

```
zeqi2026/
└── user-management/                    # 📁 项目根目录（直接 cd 进这里执行命令）
    ├── app.py                          # 🚀 后端服务入口（≈ Spring Boot 的 Application.java + Controller）
    ├── init_db.sql                     # 💾 数据库初始化脚本（建 users 表 + 插入测试 admin）
    ├── requirements.txt                # 📦 Python 依赖清单（Flask + Flask-Cors）
    └── users.db                        # 📀 SQLite 数据库单文件（首次运行 app.py 自动生成）
```

### 2.2 3 层分层架构（经典后端分层，面试必画！）

```
                     ┌─────────────────────────────────────────────────┐
                     │   🧪 客户端：Postman / 浏览器 / Apifox          │
                     │   (发起 HTTP 请求，带 Headers + Body)            │
                     └───────────────────────┬─────────────────────────┘
                                             │
                      ┌──────────────────────▼──────────────────────┐
                      │   🚀 Flask 内置 Web 服务器 (port 5000)       │
                      │   Werkzeug → 解析 HTTP 字节流 → request 对象│
                      └──────────────────────┬──────────────────────┘
                                             │
┌──────────────────────┐    ┌────────────────▼─────────────────┐    ┌──────────────────────┐
│  ① Presentation 层   │    │  ② Business Logic 业务层 (核心)   │    │  ③ Data Access 数据层 │
│  (Controller 路由)   │◄──►│  (Service 逻辑：校验/查重/加密…)   │◄──►│  (DAO / Mapper)      │
│  app.py @app.route   │    │  所有 if / else / 业务规则写这里  │    │  SQL + sqlite3 驱动   │
│  - 接收 JSON 入参     │    │  - 参数非空校验                   │    │  - ? 占位符防注入     │
│  - 返回统一 JSON 格式 │    │  - UNIQUE 冲突 409                │    │  - row_factory 映射   │
│  - HTTP 状态码设置    │    │  - 登录态 sessionId ↔ userId     │    │  - 增删改查 CRUD      │
└──────────────────────┘    └──────────────────────────────────┘    └───────────┬──────────┘
                                                                                │
                                                              ┌─────────────────▼──────────────────┐
                                                              │  💾 数据存储层：SQLite3 (users.db) │
                                                              │  • 支持事务 / 行锁 / ACID / 索引    │
                                                              │  • 单文件零配置，学习场景完美！     │
                                                              └────────────────────────────────────┘
```

### 2.3 7 个 API 汇总表（照着 Postman 填 URL/Method！）

| # | API 功能 | HTTP 方法 | 完整 URL | 登录态 | 请求体 JSON 示例 | 成功返回 | 常见失败码 |
|---|---|---|---|---|---|---|---|
| **1** | 注册 | `POST` | `http://127.0.0.1:5000/api/auth/register` | ❌ | `{"username":"zhangsan","password":"123456"}` | **201** Created + user 对象 | 400 参数空 / 409 用户名重复 |
| **2** | 登录 | `POST` | `http://127.0.0.1:5000/api/auth/login` | ❌ | `{"username":"admin","password":"admin123"}` | **200** OK + sessionId | 400 参数空 / 401 账号密码错 |
| **3** | 查看自己 | `GET` | `http://127.0.0.1:5000/api/users/me` | ✅ | **无 Body** | **200** OK + 当前用户 | 401 session 无效 |
| **4** | 用户列表(加分) | `GET` | `http://127.0.0.1:5000/api/users` | ✅ | **无 Body** | **200** OK + {total, list} | 401 未登录 |
| **5** | 改用户名(加分) | `PATCH` | `http://127.0.0.1:5000/api/users/me/username` | ✅ | `{"username":"zhangsan_new"}` | **200** OK + 更新后的 user | 401 / 409 新名冲突 |
| **6** | 改密码(加分) | `PATCH` | `http://127.0.0.1:5000/api/users/me/password` | ✅ | `{"oldPassword":"admin123","newPassword":"new456"}` | **200** OK + 提示重登录 | 401 原密码错 / 400 新旧相同 |
| **7** | 注销账号(加分) | `DELETE` | `http://127.0.0.1:5000/api/users/me` | ✅ | `{"password":"admin123"}` | **204** No Content | 401 确认密码错 |

> 🔐 **登录态机制（招新题核心要求！）**
> - 登录成功后，后端 `Sessions[uuid4()] = userId` 保存对应关系（存在内存 dict 里）
> - 前端把 `sessionId` 存好（LocalStorage / Cookie）
> - 调 **3~7 需要登录**的接口时，必须在 **HTTP 请求头 Header** 里加：
>   ```
>   Key:   sessionId
>   Value: a1b2c3d4-5678-90ef-ghij-klmnopqrstuv   ← 登录返回的那串
>   ```
> - 后端 `@login_required` 装饰器先查 Sessions：查不到 → 直接 **401 Unauthorized**（符合题目要求）

---

---

## 3. 代码逐段讲解：`app.py` 从 0 到 1

> 代码文件路径：[app.py](file:///e:/my_file/zeqi2026/user-management/app.py)
> 下面按代码分块给你讲"写了啥 + 为啥这么写 + 面试考啥"

### 3.1 第 1 块：import 导入依赖

```python
from flask import Flask, request, jsonify        # Flask 三件套 = SpringMVC 的 @Controller + HttpServletRequest + @ResponseBody
from flask_cors import CORS                        # 跨域 = Java Web 的 CorsFilter / @CrossOrigin
import sqlite3, os, uuid, random, hashlib          # Python 标准库（全自带免安装）
from datetime import datetime
from functools import wraps                         # 写装饰器必备（保留函数元信息）
```

**学习要点：**
- Flask 是"微框架"（Micro-framework）：自带功能不如 Spring Boot 全，但胜在**简单透明，零基础一眼能看明白执行流程**（完美适合招新题这种小项目学习）
- Java 对应的框架映射：
  | Python Flask | Java Spring Boot 生态 | 作用 |
  |---|---|---|
  | `Flask(__name__)` | `@SpringBootApplication SpringApplication.run()` | 创建 Web 应用实例 |
  | `@app.route()` | `@GetMapping/@PostMapping` | URL 路由映射 |
  | `request.get_json()` | `@RequestBody UserDTO dto` | 解析 JSON 请求体 |
  | `jsonify(obj)` | `ResponseEntity.ok(dto)` | 返回 JSON 响应 + 自动加 Content-Type |
  | `@login_required` 自定义装饰器 | `HandlerInterceptor + preHandle()` | AOP 切面统一登录校验 |
  | `sqlite3.connect()` | `JdbcTemplate / Mybatis Mapper` | DB 连接 & SQL 执行 |

---

### 3.2 第 2 块：全局配置（app + DB + Sessions 存储）

```python
app = Flask(__name__)
CORS(app, origins="*", supports_credentials=True)  # origins="*" 生产必须改成具体域名！

BASE_DIR = os.path.dirname(os.path.abspath(__file__))   # os.path 三板斧：取当前 py 文件所在目录绝对路径
DB_PATH = os.path.join(BASE_DIR, "users.db")            # 拼接出 users.db 的完整路径

Sessions = {}                                            # 🔐 核心数据结构：dict( sessionId -> userId )
                                                         # 等价 Java: Map<String, Long> SESSIONS = new ConcurrentHashMap<>()
SESSION_HEADER = "sessionId"                             # 请求头 key，和题目要求一致（写常量方便改）
```

**面试考点 Sessions 存储：**

| 存储方式 | 适用场景 | 优缺点 | 本项目用哪种？ |
|---|---|---|---|
| **内存 dict**（本项目） | 单机 + 学习演示 | ✅ 写起来 1 行；❌ 重启丢失、多机负载均衡失效（session sticky？） | ✅ 用这个（零配置） |
| **Redis**（生产主流） | 集群部署 | ✅ 可设置过期 TTL、多进程共享、支持黑户封禁黑名单；❌ 要额外搭 Redis | 学有余力加上（用 `redis-py` 库） |
| **JWT Token**（前后端分离流行） | 移动端 / 分布式 | ✅ 服务端**无状态**不用存，客户端自己存载荷；❌ 无法主动吊销、Token 体积大 | 2.md API 设计里讲过 |
| **DB session 表** | 对持久化要求高 | ✅ 重启不丢；❌ 每次请求查库性能差（一般加 Redis 缓存） | 大型传统项目用 |

---

### 3.3 第 3 块：DB 工具函数（DAO 层）

```python
def get_db_conn():
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row          # ✨ 神之一手：让查询结果可以用 row["username"] 取，不用 row[0] 下标
    return conn

def init_database():
    conn = get_db_conn()
    with open(os.path.join(BASE_DIR, "init_db.sql"), "r", encoding="utf-8") as f:
        conn.executescript(f.read())         # 执行整份 SQL（含 ; 分隔的多条语句）
    conn.commit()
    conn.close()

def row_to_dict(row: sqlite3.Row) -> dict:
    return {                                   # 🛡️ 安全过滤：手动列白名单，password 永远不进返回体
        "id": row["id"],
        "username": row["username"],
        "created_at": row["created_at"],
        "updated_at": row["updated_at"]
    }
```

**为什么必须用 row_to_dict 白名单？**（面试安全题）
- 如果你图省事 `return dict(row)` 直接整行转，会把 `password` 字段一起序列化发回前端！
- 抓包工具（Fiddler / Charles / Chrome DevTools）一眼能看到。**安全红线：所有用户相关的 DTO 必须手写白名单。**

---

### 3.4 第 4 块：`@login_required` 登录校验装饰器（⭐ 面试高频手写题）

```python
def login_required(func):
    @wraps(func)
    def wrapper(*args, **kwargs):
        # Step 1: 从 Header 读 sessionId（招新题明确要求用 Header 传）
        session_id = request.headers.get(SESSION_HEADER, None)

        # Step 2: 不存在 / 查不到 = 401（题目明确要求的返回码！）
        if not session_id or session_id not in Sessions:
            return jsonify({
                "code": 401, "message": "未登录或 sessionId 无效，请重新登录", "data": None
            }), 401

        # Step 3: 校验通过，把 userId 注入 request 方便下游用
        request.current_user_id = Sessions[session_id]
        return func(*args, **kwargs)
    return wrapper
```

**这个装饰器干的事 = Spring 里 3 件事合并：**
1. `HandlerInterceptor.preHandle()` → 拦截做前置校验
2. `ThreadLocal<Long> CURRENT_USER` → 存当前用户 ID（线程隔离）
3. `@ResponseStatus(HttpStatus.UNAUTHORIZED)` + 全局异常处理 → 统一 401 返回

---

### 3.5 第 5 块（核心）：7 个路由接口（挑 3 个最经典的讲，剩下 4 个同理）

#### 🟢 接口 1：`POST /api/auth/register` 注册（201 / 409）

```python
@app.route("/api/auth/register", methods=["POST"])
def register():
    data = request.get_json(silent=True)                  # silent=True：JSON 非法返回 None，不抛 500
    if not data: return jsonify(...), 400
    username = (data.get("username") or "").strip()        # or + strip：双保险处理 None / 全空格用户名
    password = (data.get("password") or "").strip()
    if not username or not password: return 400

    conn = get_db_conn()
    try:
        # ① UNIQUE 查重（也可以直接 INSERT 捕获 IntegrityError，各有优劣：先查可读性好；直接捕异常少一次 DB roundtrip）
        cursor.execute("SELECT id FROM users WHERE username = ?", (username,))
        if cursor.fetchone(): return jsonify(...), 409    # 409 Conflict = 资源冲突（用户名重复）

        # ② 插入（? 占位符防 SQL 注入！第二个参数必须是 tuple，所以单个参数要写成 (x,) 加逗号）
        cursor.execute(
            "INSERT INTO users (username, password) VALUES (?, ?)",
            (username, password)
        )
        conn.commit()                                     # DML 必须 COMMIT 才真正落盘！（autocommit 默认关）
        new_id = cursor.lastrowid                         # SQLite = MySQL 的 SELECT LAST_INSERT_ID()

        # ③ 查回返回（201 Created = RESTful 语义"创建成功"，比 200 更精准）
        cursor.execute("SELECT * FROM users WHERE id = ?", (new_id,))
        return jsonify({"code":201, "message":"注册成功", "data": row_to_dict(cursor.fetchone())}), 201
    finally:
        conn.close()                                      # finally 确保无论成功异常都关连接（连接池 later）
```

#### 🔴 接口 2：`POST /api/auth/login` 登录（返回 sessionId）

```python
# ... 参数校验省略 ...
cursor.execute("SELECT * FROM users WHERE username = ?", (username,))
row = cursor.fetchone()
if not row or row["password"] != password:
    return jsonify({... "用户名或密码错误" ...}), 401     # ⚠️ 不区分是"用户名不存在"还是"密码错"——
                                                         # 安全最佳实践：防止攻击者"枚举用户名"（用字典扫注册过的号）
session_id = str(uuid.uuid4())                            # UUID4：随机生成，几乎不可能碰撞（122 位随机）
Sessions[session_id] = row["id"]                          # 存进内存 dict
user_info = row_to_dict(row)
user_info["sessionId"] = session_id                       # 返回给前端（前端存 localStorage）
return jsonify({"code":200, "message":"登录成功", "data": user_info}), 200
```

#### 🟣 接口 7：`DELETE /api/users/me` 注销账号（高危操作 → 双重确认密码）

```python
@app.route("/api/users/me", methods=["DELETE"])
@login_required
def delete_account():
    data = request.get_json(silent=True) or {}
    confirm_pwd = (data.get("password") or "").strip()     # ① 二次确认密码（注销 = 删数据，高危操作）
    # 校验密码正确？
    if row["password"] != confirm_pwd:
        return 401, "密码确认失败"
    cursor.execute("DELETE FROM users WHERE id = ?", (user_id,))
    conn.commit()
    # ② 注销后 = 这个用户的所有 session 全部失效（踢掉其他设备登录态）
    to_del = [k for k, v in Sessions.items() if v == user_id]
    for k in to_del: del Sessions[k]
    return 200 / 204
```

---

---

## 4. 数据库表设计：`init_db.sql` 详解

> 代码文件：[init_db.sql](file:///e:/my_file/zeqi2026/user-management/init_db.sql)

```sql
CREATE TABLE IF NOT EXISTS users (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,    -- 主键：自增（SQLite 的 INTEGER PK = 自增别名 ROWID）
    username    VARCHAR(50)  NOT NULL UNIQUE,         -- 用户名：加 UNIQUE 约束 = 数据库层面兜底防重
    password    VARCHAR(255) NOT NULL,                -- 题目允许明文；生产必须存 bcrypt($2b$12$...) 哈希值
    created_at  TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,   -- 自动填创建时间
    updated_at  TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP    -- 应用层手动 UPDATE 时再改（SQLite 没有 ON UPDATE CURRENT_TIMESTAMP 语法）
);
```

### 🔑 约束设计面试考点（和 3.md「范式 + 约束」联动）

| 约束 | 作用 | 不加会怎样？ |
|---|---|---|
| `PRIMARY KEY (id)` | 主键非空唯一，聚簇索引按 id 排序存 | 无主键=InnoDB 自动生成隐藏 6 字节 ROWID，业务引用混乱 |
| `UNIQUE (username)` | 唯一约束 + 自动建唯一索引 | 并发下两个请求同时注册"zhangsan"→ DB 两条重名数据，后续登录查两行报错 |
| `NOT NULL` | 字段不允许空值 | 有人注册不传 username → DB 里存 NULL → 登录时 NULL = NULL 三值逻辑混乱 |
| `DEFAULT CURRENT_TIMESTAMP` | 插入时自动赋值 | 忘了传时间 → 时间列为 NULL，后面按时间排序全错 |

> 📌 **SQLite vs MySQL 小差异**：
> - SQLite：`AUTOINCREMENT`（严格不重用）和不加的"自增"有区别；MySQL 只有 `AUTO_INCREMENT`
> - SQLite 没有 `ON UPDATE CURRENT_TIMESTAMP`，所以改密码/改用户名时要手动带 `updated_at = ?`
> - SQLite 的 VARCHAR(50) 其实不限制长度（兼容性写了也不校验），但写好习惯方便以后切 MySQL 不改代码

---

---

## 5. 🔥 Postman 接口测试 · 手把手图文教程（重点！共 7 个接口+异常场景）

> 🎯 **这一节是本项目的灵魂**：招新题最后一条明确要求「请使用 Postman 测试接口，并附上测试结果截图」。
> 下面按顺序**先装工具 → 建 Collection → 7 个接口挨个点**，每个接口都告诉你"要截图的位置（红框标出来）"，你最后把截图贴在 Word/PDF 里就能交。

### 5.0 前置准备（一次性做）

#### ① 安装 Postman
- 官网下载：https://www.postman.com/downloads/ → 选 Windows 64 位安装版
- 第一次打开会让你登录/注册账号：**可以直接点「Skip and go to the app」跳过**（免费版功能完全够用）

#### ② 先把 Python 项目跑起来
打开 **PowerShell / CMD**，依次执行 3 行命令：
```powershell
cd e:\my_file\zeqi2026\user-management      # ① 进入项目目录
pip install -r requirements.txt             # ② 装 Flask + Flask-Cors（只装一次）
python app.py                                # ③ 启动服务 → 看到 "[DB OK] 数据库初始化完成" + 🎉 启动成功 就 OK
```
看到下面这种就说明服务正常了，**这个窗口别关**（关了 = 服务器停止 = Postman 调不通报 ECONNREFUSED）：
```
[DB OK] 数据库初始化完成，文件路径：E:\my_file\zeqi2026\user-management\users.db
======================================================================
🎉  用户管理系统 · Flask 后端启动成功 🎉
📍  本地访问地址：http://127.0.0.1:5000
🛠️   测试账号（init_db.sql 初始化）：admin / admin123
======================================================================
```

#### ③ 在 Postman 里建一个「Collection 文件夹」方便整理
1. 左侧 Collections → 点 **+** 号新建 Collection
2. 起名：`用户管理系统-择栖招新2026`，Description 可以写 `Post:5000 SQLite Flask`
3. **（神操作）设置 Collection 变量，后面不用反复复制粘贴 URL 前缀和 sessionId：**
   - 点 Collection 名字 → 切到 **Variables** Tab → 加 2 个变量：
   | Variable | Initial value | Current value |
   |---|---|---|
   | `baseUrl` | `http://127.0.0.1:5000` | 同左 |
   | `sessionId` | （空，登录后再填） | （空） |
   - 保存（Ctrl+S）后，后续 URL 写 `{{baseUrl}}/api/auth/register` 即可，一劳永逸！

---

### 5.1 ✅ 接口 1：注册（POST /api/auth/register）→ 截图【1】

#### 操作步骤：
1. 左侧 Collection 点 **Add request** → 起名：`1. 注册（成功）`
2. Method 下拉框选 **POST**，URL 填：`{{baseUrl}}/api/auth/register`
3. 切到 **Body** Tab → 选 **raw** → 最右侧下拉选 **JSON**（这步新手最容易错！默认是 Text，后端解析不到 → 报 400）
4. 粘贴 JSON：
   ```json
   {
     "username": "zhangsan",
     "password": "123456"
   }
   ```
5. 右上角蓝色 **Send** 按钮 🔵

#### 你要看到的结果（= 截图【1】内容）：
```
┌─ Status: 201 Created ──────────────────────────────────────────────────────┐ ← 红框①：201！不是200
│ Time: 32ms   Size: 280B                                                     │
├─ Body（Pretty/JSON 视图Tab）───────────────────────────────────────────────┤ ← 红框②：Body JSON
│ {                                                                           │
│   "code": 201,                                                              │
│   "message": "注册成功",                                                    │
│   "data": {                                                                 │
│     "id": 2,                                                                 │
│     "username": "zhangsan",                                                 │
│     "created_at": "2026-09-04 10:00:00",                                   │
│     "updated_at": "2026-09-04 10:00:00"                                    │
│   }                                                                         │
│ }                                                                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 🔁 再测异常场景（截图【1-b】：409 重名）
- 不改任何东西，直接再点一次 Send（同一个 zhangsan 再注册一次）
- **期望**：Status **409 Conflict**，Body 里 message = `用户名 'zhangsan' 已存在，请换一个` ✅（题目要求的边界情况）

> 📌 **截图怎么贴？**
> - 用 **Win + Shift + S** 打开 Windows 截圖工具 → 框选 Postman 整个请求+响应区域
> - 保存到桌面 → 贴进 Word/Onenote，每张图下方加「图 1-1：注册成功接口（201 Created）」

---

### 5.2 ✅ 接口 2：登录（POST /api/auth/login）→ 截图【2】

#### 操作步骤：
1. 新建 Request 起名：`2. 登录（成功）`
2. **POST** `{{baseUrl}}/api/auth/login`
3. Body → raw → JSON：
   ```json
   {"username": "admin", "password": "admin123"}   ← 用 init_db.sql 自带的 admin 测试账号
   ```
4. Send

#### 你要看到的结果（= 截图【2】）：
```
┌─ Status: 200 OK ────────────────────────────────────────────────────────────┐
├─ Body（JSON）───────────────────────────────────────────────────────────────┤
│ {                                                                           │
│   "code": 200,                                                              │
│   "message": "登录成功",                                                    │
│   "data": {                                                                 │
│     "id": 1,                                                                │
│     "username": "admin",                                                    │
│     "created_at": "...",                                                    │
│     "sessionId": "a1b2c3d4-5678-90ef-ghij-klmnopqrstuv"  ← 红框：这串要复制！ │
│   }                                                                         │
│ }                                                                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### ⚡ 关键一步：存 sessionId 到 Collection 变量
- 复制响应体里的 `sessionId`（不带引号）
- 点 Collection → Variables → 粘贴到 `sessionId` 变量的 Current Value 列 → Save

#### 🔁 测异常（截图【2-b】：401 账号错）
- 把请求体 password 改成 `wrongpassword` → Send
- 期望：**401 Unauthorized**，message=`用户名或密码错误`（故意不告诉你是哪错了，防爆破 ✅）

---

### 5.3 ✅ 接口 3：查看自己（GET /api/users/me）→ 截图【3】重点！必须传 Header

#### 操作步骤（最重要的一步：加 Header！）：
1. 新建 Request：`3. 查看当前用户（带sessionId，成功）`
2. **GET** `{{baseUrl}}/api/users/me`
3. **⚠️ 切到 Headers Tab** → Key 列填：`sessionId`，Value 列填：`{{sessionId}}`（引用变量，或直接粘贴上一步复制的）
4. **Body 不用填（GET 正常不带 Body）**，Send

#### 成功响应（截图【3】）：
```
Status: 200 OK → data 里是 admin 用户对象（id=1）
```

#### 🔁 两个必测异常场景（题目明确要求：查不到/空 sessionId 返回 401）：

| 场景 | 操作 | 预期 = 截图【3-b】 |
|---|---|---|
| **不传 sessionId** | Headers 里把 sessionId 那一行**取消勾选**→ Send | Status **401** + message="未登录或 sessionId 无效" ✅（题目要求！） |
| **传错 sessionId** | sessionId Value 填 `fake-wrong-id-123` → Send | Status **401**（同上）✅ |

---

### 5.4 ✅ 接口 4：查看用户列表（GET /api/users）+ 改用户名（PATCH）+ 改密码（PATCH）+ 注销（DELETE）→ 截图【4】~【7】

> 操作套路完全一样，只改 Method / URL / Body 即可，列表如下（**全部都要带 sessionId Header！**）：

| 截图编号 | 请求名（建议） | Method | URL | Body JSON | 成功 Status |
|---|---|---|---|---|---|
| 【4】 | 4. 用户列表（加分） | **GET** | `{{baseUrl}}/api/users` | 无 | 200 → data = `{"total": 2, "list": [{admin},{zhangsan}]}` |
| 【5】 | 5. 修改用户名（加分） | **PATCH** | `{{baseUrl}}/api/users/me/username` | `{"username":"zhangsan_new"}` | 200 → username 变成新的 |
| 【6】 | 6. 修改密码（加分） | **PATCH** | `{{baseUrl}}/api/users/me/password` | `{"oldPassword":"admin123","newPassword":"newpass456"}` | 200 → 再调任何接口就 401 了（改密码踢下线） |
| 【7】 | 7. 注销账号（加分） | **DELETE** | `{{baseUrl}}/api/users/me` | `{"password":"newpass456"}`（二次确认） | 200/204 → 再查 DB 那一行没了 |

> 💡 **截图小技巧（给交作业加分的细节）**：
> - 每次 Send 完，Postman 右上角会自动显示请求时间/大小/状态码，这些都要截图进去（证明真的调过，不是伪造的 JSON）
> - **改密码 / 注销**后，记得再截一张「调用 /users/me → 401」的截图，证明 session 真的被清了（加分点！）

---

### 5.5 🧪 完整测试用例 Checklist（交作业前打勾，保证题目第 6 步「边界情况」全覆盖）

```
✅ 【注册】
  [正常] 新用户名 zhangsan → 201 + DB 新增
  [异常] 同名再注册一次  → 409 Conflict            ← 题目要求
  [异常] username 为空字符串 " "  → 400

✅ 【登录】
  [正常] admin / admin123  → 200 + 拿到 sessionId
  [异常] 密码错 → 401 Unauthorized
  [异常] 空密码 → 400

✅ 【查看自己】重点！题目明确要求
  [正常] 带正确 sessionId Header → 200 + 自己信息
  [异常] 不带 sessionId        → 401             ← 题目明确要求
  [异常] 带伪造 sessionId      → 401             ← 题目明确要求

✅ 【用户列表】(选做)
  [正常] 带 sessionId → 200 + total / list

✅ 【改用户名】(选做)
  [异常] 改成已存在的 "zhangsan" → 409

✅ 【改密码】(选做)
  [异常] 原密码错 → 401
  [异常] 新密码=原密码 → 400

✅ 【注销】(选做)
  [异常] 确认密码错 → 401
  [正常] 确认密码对 → 204
```

---

---

## 6. 最终运行 & 提交检查清单

### 6.1 本地一键启动命令备忘录

```powershell
# ========== 第一次部署（执行 1 次就行） ==========
cd e:\my_file\zeqi2026\user-management
pip install -r requirements.txt

# ========== 每次开始开发 / 调接口 ==========
cd e:\my_file\zeqi2026\user-management
python app.py              # 启动成功后别关窗口！Postman 用 http://127.0.0.1:5000
```

### 6.2 项目提交清单（对应招新题 Part2 的「提交内容」要求）

| # | 要提交的东西 | 本项目对应位置 | 检查打勾 |
|---|---|---|---|
| 1 | GitHub 仓库 URL（写 .txt） | `https://github.com/yaya7777777/zeqi2026.git`（之前已连好）| ✅ 直接 git push |
| 2 | **代码文件**（用户管理系统） | `user-management/` 整个文件夹（app.py + init_db.sql + requirements.txt）| ✅ |
| 3 | **本说明文档**（本文档 4.md） | `4.md`（含 2 道简答题 + Postman 步骤） | ✅ |
| 4 | **Postman 接口测试截图**（题 3 明确要求） | 建议新建文件夹 `user-management/screenshots/`，按「5.1~5.7」顺序命名 1-1/1-b/2/2-b/3/3-b… 放好 | ⚠️ **你自己截一下！我没法访问你桌面截图** |
| 5 | （可选加分）Postman Collection JSON 导出 | Postman 点 Collection → 右上角 `…` → Export → Collection v2.1 → 导出为 `user-management/用户管理系统-择栖招新2026.postman_collection.json` | ➕ 建议导，评委双击就能导入直接跑 |

### 6.3 Git 一键推送（之前连好 origin，只需 3 行）

```powershell
cd e:\my_file\zeqi2026
git add -A
git commit -m "feat: 完成用户管理系统(7 API + 4.md教程+Postman测试文档)"
git push origin main
```

---

> 🎉 **恭喜！** 看到这里，你已经：
> - ✔️ 背会了「注册 9 步行程 + 持久化 7 个维度」两道高频面试简答题
> - ✔️ 从 0 到 1 手写了一个符合 RESTful 规范、有完整登录态的用户管理系统（7 个 API + 边界异常全处理）
> - ✔️ 知道每个接口在 Postman 里怎么填，截图截哪一块
> 最后就差你**打开 Postman 实际点一遍 Send + 截图保存**啦，加油！🚀
