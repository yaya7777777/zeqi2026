server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zeqi_user?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
app:
  jwt:
    secret: change-this-to-a-long-random-secret-key-2026-zeqi
    expiration-seconds: 7200
logging:
  level:
    com.zeqi.user: INFO
