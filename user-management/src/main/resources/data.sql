-- =====================================================================================================================
-- 📘 data.sql = Spring Boot 启动时在 schema.sql 之后执行，用来插"初始测试数据"
-- =====================================================================================================================
-- MERGE INTO = H2 的"插入或忽略"语法（等价 SQLite 的 INSERT OR IGNORE / MySQL 的 INSERT IGNORE）
-- 保证即使重复启动项目，admin 测试账号也只会插 1 条，不会报 UNIQUE 冲突导致启动失败。
MERGE INTO users (id, username, password) KEY(id) VALUES (1, 'admin', 'admin123');
